## 🪻 Prose Release

{% if not is_tag -%}
Build dry-run from {{ commit_link }}.
{% elif publish == "success" -%}
Published [*Prose* {{ ref }}]({{ pypi_url }}) from {{ commit_link }}, tagged {{ tag_link }}.
{% elif prepub_failed -%}
Pre-publish checks failed at {{ commit_link }} for tag {{ tag_link }}. Publish skipped.
{% else -%}
Publish failed for tag {{ tag_link }} at {{ commit_link }}.
{% endif %}

### Builds

| Platform | Target | Status |
|---|---|---|
{% for a in platforms %}
| {{ a.label }} | {{ a.target }} | {{ a.mark }} |
{% endfor %}
{% if is_tag and publish == "success" %}

### Install

```bash
pip install prose-formatter=={{ ref }}
```

### Verify

Every wheel ships a [PEP 740 attestation](https://peps.python.org/pep-0740/) visible on the [PyPI project page]({{ pypi_url }}), plus a [SLSA build provenance attestation](https://slsa.dev/spec/v1.0/provenance) verifiable through the GitHub CLI:

```bash
gh attestation verify <wheel> --repo Jybbs/prose
```
{% endif %}

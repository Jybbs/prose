#!/usr/bin/env -S bash -euo pipefail
#MISE description="Build the crate in debug mode"

case "${1:-all}" in
  all) cargo build --all-targets --locked ;;
  bin) cargo build --bin prose --locked ;;
  *)   echo "usage: mise build [bin]" >&2; exit 2 ;;
esac

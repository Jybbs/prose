#!/usr/bin/env -S bash -euo pipefail
#MISE alias       = "probe"
#MISE description = "Build the probe binary, the corpus binaries, and the harnesses the settle, subset, import, and delta sweeps run on"

cargo build --bins --locked -p prose -p prose_corpus --profile probe
cargo test --locked --no-run --profile probe --test corpus --test imports --test settle

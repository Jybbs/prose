#!/usr/bin/env -S bash -euo pipefail
#MISE alias       = "probe"
#MISE description = "Build the probe binary and the corpus harnesses the settle, subset, and delta sweeps run on"

source "$MISE_PROJECT_ROOT/.mise/lib/delta.bash"

built_binary probe > /dev/null
cargo test --locked --no-run --profile probe --test corpus --test settle

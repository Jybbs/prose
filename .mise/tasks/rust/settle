#!/usr/bin/env -S bash -euo pipefail
#MISE alias       = "settle"
#MISE description = "Sweep a corpus at every line length for rewrites a second pass would change and fixes the output never took"

source "$MISE_PROJECT_ROOT/.mise/lib/corpus.bash"

corpus=$(corpus_root "${1:-}")
PROSE_SETTLE_CORPUS="$corpus" cargo test --locked --test corpus

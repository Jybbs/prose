#!/usr/bin/env -S bash -euo pipefail
#MISE alias       = "subsets"
#MISE description = "Probe every rule alone and every ordered rule pair for one-pass settling over a corpus"

source "$MISE_PROJECT_ROOT/.mise/lib/corpus.bash"

corpus=$(corpus_root "${1:-}")
PROSE_SETTLE_CORPUS="$corpus" cargo test --locked --release --test settle

#!/usr/bin/env -S bash -euo pipefail
#MISE depends     = ["site:coverage"]
#MISE description = "Upload the docs-site coverage report to Codecov"

codecovcli upload-process --file site/coverage/lcov.info --flag site --sha "$SHA"

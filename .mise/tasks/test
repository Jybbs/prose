#!/usr/bin/env -S bash -euo pipefail
#MISE description="Run the test suites including `insta` snapshots, scoped by a `rust` or `site` argument"
#MISE wait_for=["bake"]

source "$MISE_PROJECT_ROOT/.mise/lib/scope.bash"
rust() { cargo test --locked; }
site() { mise run build bin && (cd site && bunx vitest run "$@"); }
scope "$@"

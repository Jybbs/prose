#!/usr/bin/env -S bash -euo pipefail
#MISE description="Generate coverage and upload it to Codecov for the Rust crate"
#MISE depends=["coverage"]
#MISE tools={"pipx:codecov-cli" = "11.2.8"}

codecovcli upload-process --flag rust --file target/lcov.info --sha "$SHA"

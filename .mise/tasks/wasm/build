#!/usr/bin/env -S bash -euo pipefail
#MISE description = "Build the `prose_wasm` member for `wasm32-unknown-unknown` under `wasm-release`"

source "$MISE_PROJECT_ROOT/.mise/lib/wasm.bash"

wasm_cargo build

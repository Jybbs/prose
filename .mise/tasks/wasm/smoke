#!/usr/bin/env -S bash -euo pipefail
#MISE alias       = "smoke"
#MISE depends     = ["site:bake", "wasm:pack"]
#MISE description = "Run the browser smoke test against the packed `site/public/wasm` module"

(cd site && bunx vitest run --project wasm)

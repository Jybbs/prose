[docs]
previewable = false
title       = "Already-Aligned Input Gets No Edit"

description = '''
The `CAPITALS` dict, the `User` class body, and the `register` signature with its `Args` docstring section each already keep their `:` on the column the rule would pick. **Input that is already aligned gets no edit**, so no gap changes and the output matches the input.
'''

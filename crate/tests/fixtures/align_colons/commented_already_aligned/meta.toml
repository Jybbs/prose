[docs]
previewable = false
title       = "An Already-Aligned Commented Dict Gets No Edit"

description = '''
Every entry of `config` carries a trailing `#` comment, and the `:` column already sits at the width set by `"timeout"`, the widest key. **A commented dict that is already aligned gets no edit**, so the output matches the input.
'''

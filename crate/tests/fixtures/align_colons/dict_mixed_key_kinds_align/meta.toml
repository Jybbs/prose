[docs]
previewable = true
title       = "Mixed Key Shapes Still Find the Widest Key"

description = '''
The keys of `mapping` mix string literals, the integer `42`, and the bare name `HEADER`. The column math runs on each key's **display width** rather than its expression kind, so all four colons land on the column set by `HEADER`, the widest key as printed.
'''

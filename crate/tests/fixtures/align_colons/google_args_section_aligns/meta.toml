[docs]
previewable = false
title       = "The `Args:` Section Aligns While Other Sections Stay Put"

description = '''
The docstring of `transfer` carries a Google-style `Args:` section listing `source`, `destination`, `amount`, and `memo`. Each entry's `:` aligns on the column set by `destination`, the widest name, while **the `Returns:` section below and the function body stay as written**.
'''

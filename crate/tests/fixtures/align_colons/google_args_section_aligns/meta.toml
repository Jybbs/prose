[docs]
previewable = false
title       = "Args Section Aligns While Other Sections Stay Put"

description = '''
The docstring of `transfer` carries a Google-style `Args:` section listing `source`, `destination`, `amount`, and `memo`. Each entry's `:` aligns to the column set by `destination`, the widest name, while **the `Returns:` section below and the function body stay untouched**.
'''

def build(source_channel, n):
    """Build the payload.

    Args:
        source_channel: the wide one.
        n: narrow.

    Returns:
        ok: a flag.
        payload: the body.
    """
    return None

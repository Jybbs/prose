[docs]
previewable = false
title       = "A Lone Type Group Settles Beside Untyped Siblings"

description = '''
Only `beta` in this `Args:` block carries a parenthesized type, so no shared type column forms and the padded gap in `beta  (int)` collapses to a single space. The `:` column seats across `alpha`, `beta`, and `gamma`, with `beta (int)` as the widest head.
'''

[docs]
previewable = false
title       = "A Skipped Entry Stays Tight and the Run Bridges It"

description = '''
The `"port"` entry carries a `# prose: skip[align-colons]` suppression. **It drops out of the alignment group and keeps its tight spacing**, whereas `"host"` and `"timeout"` bridge across the skipped line and align on the `:` column the two of them set.
'''

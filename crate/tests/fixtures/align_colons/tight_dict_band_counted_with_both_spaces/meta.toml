[docs]
previewable = true
title       = "A Tight Dict Band Is Measured With Both Inserted Spaces"

description = '''
Each entry sits flush against its `:`, so the aligned band inserts a space before the colon and another after it. Counting both keeps the long-valued entry out of the shared column instead of emitting it past `code-line-length`.
'''

[docs]
previewable = false
title       = "An Unlimited Cap Folds the Type Column Together"

description = '''
`max-shift = false` lifts the spread budget, so the outlier `wide_name_partitions_the_type_group_column` breaks out under the default folds back in and every type group seats on one column however far the widest name reaches.
'''

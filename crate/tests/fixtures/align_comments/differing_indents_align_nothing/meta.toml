[docs]
previewable = true
title       = "Rows at Differing Indents Keep Their Own Gaps"

description = '''
`flush()` sits indented inside the `if ready:` branch while `compact_the_index()` sits at the top level, so the two annotated rows open at different indents and a shared column would land where neither row's code ends. The rule aligns nothing, leaving both comments the two-space gap they already carry.
'''

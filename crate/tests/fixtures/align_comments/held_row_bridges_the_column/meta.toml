[docs]
previewable = true
title       = "A Skipped Row Bridges the Rows Around It"

description = '''
`compact_the_index()` carries a `# prose: skip[align-comments]` directive, so its comment neither moves nor ends the run. `seed()` and `flush()` bridge across the held row and settle on one column measured from their own widths alone.
'''

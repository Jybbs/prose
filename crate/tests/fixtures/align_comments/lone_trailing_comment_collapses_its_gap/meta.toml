[docs]
previewable = true
title       = "A Lone Trailing Comment Collapses to the Floor"

description = '''
`flush()` carries the only trailing comment in the module, and one annotated line is not a run, so no shared column exists. The wide gap before its `#` settles back to the two-space floor, the same spacing a row gets when it partitions out of a column.
'''

[docs]
previewable = true
title       = "A Uniform Gutter Tightens onto the Shared Column"

description = '''
The three `DECODING_TABLE` entries carry a wide hand-set gutter that already lines their comments up in one column. The shared column still resolves to two past the widest row, so the gutter pulls in as a block and the alignment it was written to create survives at the tighter offset.
'''

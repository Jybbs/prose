[docs]
previewable = true
title       = "Already-Aligned Source Gets No Edit"

description = '''
The `and` chain over `foo`, `bar`, and `qux` already has each `==` one space past the widest left operand. **The rule emits no edit**, so the source passes through unchanged.
'''

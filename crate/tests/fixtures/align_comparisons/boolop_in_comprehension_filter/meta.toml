[docs]
previewable = false
title       = "BoolOp in a Comprehension `if` Filter Aligns"

description = '''
The `if` filter of the `filtered` comprehension holds a multi-line `and` chain, and it **aligns like any other multi-line `BoolOp`**. The `==` after `x.kind` and `x.weight` settle onto one shared column inside the filter's parentheses.
'''

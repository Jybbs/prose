[docs]
previewable = true
title       = "A Multi-Line Operand Breaks the Run"

description = '''
`foo` compares against a parenthesized `1 + 2` whose range **spans multiple source lines, which breaks the run**, so the `foo` row takes no padding. `bar_baz == 3` and `qux == 4` below it align among themselves.
'''

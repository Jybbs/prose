[docs]
previewable = false
title       = "A Single-Line BoolOp Passes Through Verbatim"

description = '''
The whole `and` chain assigned to `x` fits on **one source line, so it passes through verbatim**. Alignment only applies across stacked rows, and a single line holds no rows to stack, so the rule makes no edit.
'''

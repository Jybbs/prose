[docs]
previewable = false
title       = "A Single-Line Chain Passes Through Unchanged"

description = '''
The whole `and` chain assigned to `x` sits on one source line. **The rule emits no edit for a single-line chain**, because alignment pads rows stacked one above another and one line has no rows to stack.
'''

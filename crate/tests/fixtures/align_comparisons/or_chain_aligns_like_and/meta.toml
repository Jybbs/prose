[docs]
previewable = true
title       = "`or`-Chains Align with the Same Column Math as `and`-Chains"

description = '''
The `or` chain over `foo`, `bar_baz`, and `quux` aligns with the **same column math** the rule applies to `and` chains. Each row's `==` settles one space past the widest left side, the `or bar_baz` row.
'''

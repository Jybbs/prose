[docs]
canonical   = true
previewable = true
title       = "Single-Comparator Operands Share One Operator Column"

description = '''
The multi-line `and` chain compares `foo`, `bar_baz`, and `quux` with one `==` apiece, and the three rows share an alignment column. Each `==` sits one space past the widest left side, the `and bar_baz` row.
'''

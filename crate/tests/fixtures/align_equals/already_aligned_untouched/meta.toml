[docs]
previewable = true
title       = "Already-Aligned Source Is Left Alone"

description = '''
The `=` of `a`, `bb`, and `ccc` already share the column one space past `ccc`, so the run is **idempotent** under the rule. No edit fires and the group passes through unchanged.
'''

[docs]
previewable = true
title       = "Already-Aligned Source Passes Through Unchanged"

description = '''
`a`, `bb`, and `ccc` arrive with their `=` already on the column one space past `ccc`. **The rule writes no edit**, so the three lines pass through exactly as written.
'''

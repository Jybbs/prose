[docs]
previewable = true
title       = "Already-Aligned Source Is Left Alone"

description = '''
The `=` of `a`, `bb`, and `ccc` already share the column one space past `ccc`, so **no edit fires** and the group passes through unchanged.
'''

[docs]
previewable = true
title       = "Function-Parameter Defaults Align on the Same Column"

description = '''
The three annotated parameters of `configure` align their defaults across the multi-line signature. The `=` column sits **one space past** the widest row, `delay: float`, so `host: str` and `port: int` take padding between the annotation and the `=`.
'''

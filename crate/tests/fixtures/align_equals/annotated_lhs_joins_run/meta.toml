[docs]
previewable = true
title       = "Type Annotations Sit inside the Alignment"

description = '''
The annotated assignment `beta: int = 2` **joins the run** with the plain bindings `alpha` and `gamma_long`. Its left-hand width runs through the annotation, so the `int` counts toward the column and its `=` lands in line with the other two.
'''

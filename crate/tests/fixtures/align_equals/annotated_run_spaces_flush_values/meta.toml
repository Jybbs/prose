[docs]
previewable = false
title       = "A Flush Annotated Run Spaces Its Values"

description = '''
`x: int=1` and `name: str=2` arrive with the `=` flush on both sides. The rule inserts the value-side space along with the name-side column padding, so both rows land on the `name: str = 2` shape with their `=` in one column.
'''

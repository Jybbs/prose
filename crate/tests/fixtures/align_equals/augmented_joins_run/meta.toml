[docs]
previewable = true
title       = "Augmented Assignments Join the Run"

description = '''
The augmented assignment `beta += 2` joins the run with the plain bindings `alpha` and `gamma_long`, aligning its `=` on the shared column. The `+` of `+=` therefore **sits one column before** the stacked equals signs.
'''

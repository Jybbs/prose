[docs]
previewable = true
title       = "Blank Lines Reset the Group"

description = '''
The blank line **splits the bindings into independent groups**, so `alpha` and `beta` settle a narrow `=` column above the gap while `long_name` and `short` settle a wider one below it. Neither side's width reaches across the blank line.
'''

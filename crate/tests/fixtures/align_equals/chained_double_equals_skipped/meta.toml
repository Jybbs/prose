[docs]
previewable = true
title       = "Chained Assignments Stay Un-Aligned"

description = '''
The chained assignment `a = b = 1` carries two `=` tokens, and neither one is the row's single alignment anchor, so the rule **skips it**. That leaves `xx = 2` standing alone, and one binding is not a run, so nothing moves.
'''

[docs]
previewable = false
title       = "A Value Continued Onto the Next Line Passes Through Unchanged"

description = '''
`total = \` continues its value `a + b` onto the next line after a backslash. **The statement passes through unchanged**, because the value-side spacing rewrite stops when the gap after `=` spans a line break.
'''

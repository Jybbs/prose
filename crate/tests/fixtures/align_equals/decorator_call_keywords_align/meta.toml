[docs]
previewable = false
title       = "A Decorator Call's Keywords Align"

description = '''
The `@register(` decorator above `handler` is already exploded, one keyword per line. Its `name`, `priority`, and `enabled` keywords align on a shared `=` column exactly as they would inside any other call, because the rule reaches every call expression, decorator position included.
'''

[docs]
previewable = false
title       = "A Decorator Call's Keywords Align"

description = '''
The `@register(` decorator above `handler` is already exploded, one keyword per line. **`name`, `priority`, and `enabled` align their `=` on one column**, because the rule reads every call expression the same way, decorator position included.
'''

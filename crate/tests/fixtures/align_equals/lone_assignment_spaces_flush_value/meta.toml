[docs]
previewable = false
title       = "A Lone Flush Assignment Gains a Space on Each Side"

description = '''
`tally=1` is the only assignment in the file, written with no space on either side of the `=`. **Each gap widens to a single space**, so the statement reads `tally = 1`, with no column to join.
'''

[docs]
previewable = false
title       = "A Flush Assignment Gains Its Value-Side Space"

description = '''
`tally=1` arrives with its `=` written tight on both sides, and as the lone assignment in the file it has no column to join. Each gap normalizes to a single space instead, so the statement lands on plain `tally = 1`.
'''

[docs]
previewable = false
title       = "Each Scope Aligns Its Own Group"

description = '''
`outer` and `answer` sit at module level, `x` and `quux` inside `foo`, and `a`, `bb`, and `ccc` in the body of `Bar`. **Each scope aligns as its own group on its own `=` column**, so no column reaches across a scope boundary.
'''

[docs]
previewable = false
title       = "A Pair Wider Apart Than `max-shift` Stays Unaligned"

description = '''
`ttl` is three characters wide and `maximum_connection_backoff_seconds` is thirty-four, a gap wider than `max-shift` allows. **Neither pads toward the other**, so both keep a single space before their `=`.
'''

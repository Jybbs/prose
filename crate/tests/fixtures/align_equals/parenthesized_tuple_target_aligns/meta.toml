[docs]
previewable = false
title       = "A Parenthesized Tuple Target Aligns"

description = '''
The tuple target `(a, b)` keeps its parentheses and its unpacking assignment aligns like any other. `(a, b)` measures six characters, parentheses included, so `cc` pads out to meet that width and both `=` land on one column.
'''

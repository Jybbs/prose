[docs]
previewable = false
title       = "Single-Line Signatures Pass Through Untouched"

description = '''
`f` declares `a: int = 1` and `bee: int = 2` on a single line, and **the signature passes through unchanged**. Defaults align only across a signature exploded onto multiple lines, so a one-line signature keeps each `=` at its natural spacing.
'''

[docs]
previewable = false
title       = "A Single-Line Signature Passes Through Unchanged"

description = '''
`f` declares `a: int = 1` and `bee: int = 2` on one line. **The signature passes through unchanged**, because defaults align only across a signature exploded onto several lines, so each `=` keeps the spacing it was written with.
'''

[docs]
previewable = false
title       = "A Parenthesized Walrus Parameter Annotation Aligns"

description = '''
A parameter whose annotation parenthesizes an assignment expression aligns its default like any other, because the rule measures the parameter annotation through its explicit parentheses so the closing `)` stays left of the rewritten gap.
'''

[docs]
previewable = false
title       = "A Lone Import Is Left Untouched"

description = '''
The blank line splits `from collections import OrderedDict` and `import json as j` into two groups of one, and alignment requires **at least two members per group**, because a lone line has no neighbor to share a column with. Neither singleton receives an edit, so the file passes through unchanged.
'''

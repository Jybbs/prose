[docs]
previewable = false
title       = "A Lone Import Is Left as Written"

description = '''
A blank line separates `from collections import OrderedDict` from `import json as j`, leaving each in a group of one. **Neither line receives an edit**, because alignment needs at least two members in a group and a lone line has no neighbour to share a column with, so the file is left as written.
'''

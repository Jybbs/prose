def describe(point):
    match point:
        case Point(): return frobnicate(alpha_argument, beta_argument, gamma_argument_x)


def summarize(point):
    match point:
        case Point():
            return frobnicate(alpha_argument, beta_argument, gamma_argument_x)

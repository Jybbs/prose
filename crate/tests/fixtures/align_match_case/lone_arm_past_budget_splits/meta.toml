[docs]
previewable = true
title       = "An Arm Too Wide to Fold Alone Splits Onto Two Lines"

description = '''
`case "rebuilt"` is the only arm in its `match`, folded with its `return` onto a line that reaches 95 columns even with its `:` flush against the pattern, past the 88-column `code-line-length`. **The body moves onto its own line beneath the `case`**, the one form of the arm that fits.
'''

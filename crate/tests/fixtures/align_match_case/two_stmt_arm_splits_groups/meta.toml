[docs]
previewable = true
title       = "Multi-Statement Arms Break the Alignment Run"

description = '''
`case "certification"` holds two statements, `icon = "scroll"` and `notes.append("verified")`, and **the multi-statement body disqualifies the arm and splits the match into sub-groups**. `case "apprenticeship"` above it collapses alone with its `:` hugging the pattern, the two-statement arm itself stays multi-line, and `"program"` and `_` below it align together on the column `"program"` sets.
'''

[docs]
previewable = false
title       = "A Function's Docstring Entries Mirror Its Held Signature"

description = '''
A signature holds its positional parameter order, so an `Args:` section whose every entry names one of those parameters **holds the same order**, mirroring the held signature. The hold owes nothing to the decorator the function happens to carry.
'''

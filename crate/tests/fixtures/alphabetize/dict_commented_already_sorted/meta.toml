[docs]
previewable = false
title       = "An Ordered Commented Dict Stays Untouched"

description = '''
The `palette` keys `"amber"`, `"cyan"`, and `"magenta"` already sit in alphabetical order, so the sort emits **no edit**. The trailing `#` comments and the magic trailing `,` come through exactly as written, leaving the dict untouched.
'''

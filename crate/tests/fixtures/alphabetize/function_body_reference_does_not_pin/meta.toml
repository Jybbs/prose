[docs]
previewable = false
title       = "A Method-Body Reference Does Not Pin the Order"

description = '''
`Alpha.run` returns `Beta()`, but that reference sits inside a method body, which executes when the method is called rather than when the class is defined. By call time both classes exist whatever order they sit in, so the reference pins nothing and `Alpha` sorts ahead of `Beta`.
'''

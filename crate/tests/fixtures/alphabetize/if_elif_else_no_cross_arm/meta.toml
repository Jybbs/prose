[docs]
previewable = false
title       = "`if` / `elif` / `else` Arms Sort Without Crossing"

description = '''
Each `if` / `elif` / `else` arm **sorts its body independently**. The arms share no sort key space, so an import named `zeta` in one arm and `alpha` in another never migrate across the arm boundary.
'''

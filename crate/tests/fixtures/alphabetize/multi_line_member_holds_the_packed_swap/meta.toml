[docs]
previewable = false
title       = "A Multi-Line Member Holds the Packed Swap"

description = '''
An in-place swap moves bare member slices between slots, and a slice spanning lines keeps its interior rows at their source columns wherever it lands. A sort that would relocate such a member holds the group in source order.
'''

[docs]
previewable = false
title       = "The `__future__` Pin Survives a Flat Import Block"

description = '''
With `group-imports` off, the run sorts as one flat block, and `from __future__ import division` still holds the first slot rather than sorting among the names below it.
'''

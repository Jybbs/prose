[docs]
previewable = false
title       = "Already-Sorted Input Emits Zero Edits"

description = '''
The imports already read `collections` before `loguru`, `Alpha` precedes `Beta`, and `Alpha`'s methods already follow the four-group order, the dunders `__init__` and `__repr__`, then the `name` property, then the private `_helper`, then the public `update`. **The rule emits zero edits** and the file passes through unchanged, because every sortable site is already in order.
'''

[docs]
previewable = false
title       = "Async Functions Sort by Name Among the Plain Functions"

description = '''
`zeta` and `beta` are `async def` and sit around the plain `alpha` and the class `Widget`. **Both sort by name among the plain functions, below the class band**, rather than keeping their original places, because `async` does not change which band a function sorts in.
'''

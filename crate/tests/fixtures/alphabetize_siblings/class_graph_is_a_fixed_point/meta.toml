[docs]
previewable = false
title       = "An Already-Ordered Class Body With a Dependency Is Left Unchanged"

description = '''
`height` and `width` already sit in name order, and `HALF` already sits below the `width` its value reads. **The output equals the input**, because the dependency-aware sort finds nothing to move.
'''

[docs]
previewable = false
title       = "A Base-Class Reference Cycle Skips the Whole Run"

description = '''
`class Alpha(Beta)` inherits from `Beta` while `class Beta(Alpha)` inherits from `Alpha`, so each class needs the other defined first. **The whole run skips and the two classes keep their source order**, because no ordering satisfies both references.
'''

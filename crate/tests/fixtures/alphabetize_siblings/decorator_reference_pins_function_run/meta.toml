[docs]
previewable = false
title       = "A Decorator Keeps the Function It Decorates Below It"

description = '''
`@register` runs the moment `def dispatch` executes, so `register` must already be defined at that point. **The pair keeps source order**, because `dispatch` would sort ahead of `register` alphabetically, and swapping them would call the decorator before it exists.
'''

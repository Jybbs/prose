[docs]
previewable = false
title       = "`del` Targets Sort Independently"

description = '''
`del registry, cache, buffer` removes each name independently, so the statement means the same thing in any order. **The targets alphabetize to `del buffer, cache, registry`**, the same source-text sort the elements of a set literal take.
'''

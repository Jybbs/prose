[docs]
previewable = false
title       = "A Derived Class Stays in the Class Band"

description = '''
`Widget` names `Gadget` as its base, so it tiers behind `Gadget` inside the class band rather than sinking below `build` and `render`, and the module reads its two classes first with `Gadget` ahead of the `Widget` that reads it.
'''

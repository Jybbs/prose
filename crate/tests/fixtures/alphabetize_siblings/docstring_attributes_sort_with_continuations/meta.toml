[docs]
previewable = false
title       = "`Attributes:` Entries Sort, Carrying Their Continuations"

description = '''
`Config`'s `Attributes:` entries **reorder alphabetically by attribute name**, settling into `retries`, `timeout`, `verbose`. The `verbose` description spills onto an indented continuation line, and that hanging line travels with its entry to the new last position.
'''

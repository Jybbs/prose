[docs]
previewable = false
title       = "`Yields:` Entries Sort When Named"

description = '''
`stream`'s `Yields:` section lists `token`, `offset`, and `record`, each in the `name: description` form. **The section reorders alphabetically to `offset`, `record`, `token`**, because every entry carries a name to sort by.
'''

[docs]
previewable = false
title       = "Trailing Comments Move With Their `__all__` Entry"

description = '''
`__all__` lists `"Widget"`, `"Button"`, and `"Frame"`, each with a trailing comment. **Sorting moves `"Widget"` from first to last and its `# the base control` comment stays attached through the move**, so each trailing comment sits beside the same export it annotated in the source and none describes the wrong name.
'''

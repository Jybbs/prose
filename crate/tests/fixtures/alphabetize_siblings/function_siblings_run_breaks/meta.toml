[docs]
previewable = false
title       = "Consecutive Free Functions Sort by Name"

description = '''
Three consecutive free functions arrive in reverse alphabetical order, `render`, `collect`, `aggregate`. **The run alphabetizes, `aggregate` rising to the top and `render` dropping to the bottom.** Inside a class body the same siblings would take the four-group method ordering instead.
'''

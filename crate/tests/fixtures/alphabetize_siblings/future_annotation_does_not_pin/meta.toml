[docs]
previewable = false
title       = "A Deferred Annotation Does Not Pin the Order"

description = '''
`Builder.make` names `Helper` in its return annotation, and under `from __future__ import annotations` every annotation is stored as a string rather than evaluated at definition time. The reference therefore never runs when the classes are defined, so it pins nothing and `Builder` sorts ahead of `Helper`.
'''

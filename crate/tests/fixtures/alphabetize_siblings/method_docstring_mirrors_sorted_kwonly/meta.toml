[docs]
previewable = true
title       = "Docstring Entries Mirror the Sorted Keyword-Only Block"

description = '''
The docstring mirrors the signature **as the rule leaves it**, not as the source wrote it. The keyword-only block re-sorts to `mode, retries=3`, the required `mode` moving ahead of the defaulted `retries`, and the `mode:` and `retries:` entries under `Args:` swap to match, keeping the docs aligned with the signature a reader scans.
'''

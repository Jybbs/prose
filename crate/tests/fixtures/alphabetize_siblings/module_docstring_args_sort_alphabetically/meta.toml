[docs]
previewable = true
title       = "A Module Docstring's Entries Sort by Name"

description = '''
A module carries no signature for its entries to mirror, so an entry-bearing section inside its docstring sorts alphabetically throughout rather than taking a parameter order from anywhere.
'''

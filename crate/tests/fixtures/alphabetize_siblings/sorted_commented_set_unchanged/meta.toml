[docs]
previewable = false
title       = "An Already-Sorted Commented Set Stays Unchanged"

description = '''
The set `x` contains `ant`, `bee`, and `cat` already in alphabetical order, each with a trailing comment. **The whole group comes back unchanged, every comment where it was**, because a collection already in order is not rewritten.
'''

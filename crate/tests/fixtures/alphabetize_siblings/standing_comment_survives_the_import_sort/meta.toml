[docs]
previewable = false
title       = "A Standing Comment Survives the Import Sort"

description = '''
The comment sits on its own line at a two-space indent matching neither import, so it attaches to neither of them.

The sort swaps `import zlib` and `import os` around it, and **the comment stays exactly where it stands** rather than travelling with either name.
'''

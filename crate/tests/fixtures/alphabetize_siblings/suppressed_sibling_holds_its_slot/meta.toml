[docs]
previewable = false
title       = "A Suppressed Sibling Holds Its Slot While the Rest Sort"

description = '''
The `# prose: off` region holds `middle` at its own slot.

`apple` and `zebra` **still trade places around it**, because the sort edits only the siblings it actually moves rather than rewriting the whole run that spans the held one.
'''

[docs]
previewable = false
title       = "Classes With No Cross-Reference Still Sort"

description = '''
`Beta` is written above `Alpha`, and neither names the other. **The pair sorts by name alone, `class Alpha` moving ahead of `class Beta`**, because no dependency pins the order.
'''

[docs]
previewable = false
title       = "`with` Blocks Sort Imports in Scope"

description = '''
The `with open("foo") as f:` body holds three `from` imports out of order.

A `with` block **recurses exactly as other compound bodies do**, so the run sorts in place to `alpha`, `beta`, `zeta` without leaking into the statements around it.
'''

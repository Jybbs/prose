[docs]
previewable = false
title       = "`with` Blocks Sort Imports in Scope"

description = '''
The `with open("foo") as f:` body contains three `from` imports out of order. **The run sorts in place to `alpha`, `beta`, `zeta`**, because a `with` block recurses exactly as other compound bodies do.
'''

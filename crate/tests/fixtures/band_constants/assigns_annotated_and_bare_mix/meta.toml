[docs]
previewable = false
title       = "Bare and Annotated Assignments Sort Together"

description = '''
`zoo: int = 3`, `apple = 1`, and `banana: str = "two"` mix annotated and bare assignments in one run. **All three sort by target name alone**, so `apple` moves to the top and `zoo` to the bottom, because an annotation makes no difference to the order.
'''

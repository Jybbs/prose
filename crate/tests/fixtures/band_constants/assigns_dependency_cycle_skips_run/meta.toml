[docs]
previewable = false
title       = "A Dependency Cycle Skips the Whole Run"

description = '''
`FORWARD` reads `BACKWARD` and `BACKWARD` reads `FORWARD`, so the run's dependency graph carries a cycle. No ordering can place each definition ahead of its use, so **the whole run skips**, leaving even the independent `MIDDLE = 0` in place.
'''

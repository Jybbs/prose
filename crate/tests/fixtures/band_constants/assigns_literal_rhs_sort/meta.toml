[docs]
previewable = false
title       = "Literal-Valued Bindings Sort within Tier Zero"

description = '''
`charlie`, `alpha`, and `bravo` each bind a string literal and none reads another, so the dependency graph inside the run is empty. **The run alphabetizes freely**, landing `alpha = "first"` above `bravo = "second"` above `charlie = "third"`.
'''

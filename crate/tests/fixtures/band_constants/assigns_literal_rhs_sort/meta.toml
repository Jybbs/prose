[docs]
previewable = false
title       = "Literal-Valued Constants Sort by Name"

description = '''
`charlie`, `alpha`, and `bravo` each bind a string literal and none reads another, so the run carries no dependency. **The three sort by name**, `alpha = "first"` above `bravo = "second"` above `charlie = "third"`.
'''

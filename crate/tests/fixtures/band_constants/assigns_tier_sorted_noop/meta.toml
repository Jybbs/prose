[docs]
previewable = false
title       = "Tier-and-Alphabetical Order Is Idempotent"

description = '''
`ALPHA` and `BRAVO` already alphabetize, and `CHARLIE = ALPHA + BRAVO` already sits below the two names it reads. The run **produces no edits**, leaving the module exactly as written.
'''

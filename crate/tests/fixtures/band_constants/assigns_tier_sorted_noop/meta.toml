[docs]
previewable = false
title       = "A Run Already in Tier Order Is Left Unchanged"

description = '''
`ALPHA` and `BRAVO` are already in name order, and `CHARLIE = ALPHA + BRAVO` already sits below the two names it reads. **The rule emits no edit**, and the module passes through exactly as written.
'''

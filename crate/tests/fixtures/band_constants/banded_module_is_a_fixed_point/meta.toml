[docs]
previewable = false
title       = "A Module Already in Band Order Is Left Unchanged"

description = '''
`BASE` and `DERIVED = BASE + 1` already lead the module below the `__future__` import, `consume` sits in the middle, and `HANDLER = consume` already trails beneath the definition it reads. **The file passes through unchanged**, because every constant already sits in its band.
'''

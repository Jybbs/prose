[docs]
previewable = false
title       = "group-constants = false Sorts by Tier and Name Alone"

description = '''
With `group-constants = false`, the band sorts by tier and name alone, so the alias `Zeta` no longer clusters ahead of the constant `ALPHA`.
'''

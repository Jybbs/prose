[docs]
previewable = true
title       = "A Heading Touching an Anchored Constant Seats Tight beneath the Band"

description = '''
`# the ceiling the scheduler reads` sits directly above `LIMIT = compute()`, so it heads that constant rather than standing apart, and `LIMIT` anchors on its call while `ALPHA` and `ZETA` sort above it. **The heading seats tight beneath the sorted pair** with its constant, the blank line the source held above it closing as the band seats the anchored member.
'''

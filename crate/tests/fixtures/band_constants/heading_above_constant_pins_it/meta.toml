[docs]
previewable = false
title       = "A Heading Above a Constant Pins It In Place"

description = '''
The `### theme colors` comment reads as a section divider rather than an ordinary note, and a constant beneath a divider is pinned to its section. `PALETTE` therefore stays under the heading instead of hoisting across it into the leading band above `build_palette`, leaving the file unchanged.
'''

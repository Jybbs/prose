[docs]
previewable = false
title       = "An Import Below a Definition Hoists Into the Import Band"

description = '''
`import os` sits beneath `def helper`, so it hoists into the import band at the top of the module. `CONFIG = helper` stays in the trailing band below the definition, because its right-hand side reads `helper` and that name must already exist when the line runs.
'''

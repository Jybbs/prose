[docs]
previewable = false
title       = "A Module Dunder Joins the Leading Band"

description = '''
`__all__ = ["helper"]` mentions `helper` only inside a string literal, so its value reads no definition and the dunder counts as dependency-clean. It **joins the leading band as an ordinary entry**, hoisting above `def helper` and sorting alongside `VERSION` with no special placement.
'''

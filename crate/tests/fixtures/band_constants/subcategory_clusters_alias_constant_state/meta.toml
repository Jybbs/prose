[docs]
previewable = true
title       = "Aliases, Constants, and Module State Cluster within a Band"

description = '''
The band mixes three kinds of entry, the type aliases `Handler` and `Interval`, the `SCREAMING_CASE` constants `DEFAULT` and `MAX`, and the lowercase module state `setting` and `threshold`. Each kind clusters together, aliases first, then constants, then state, alphabetized within each group.
'''

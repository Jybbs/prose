[docs]
previewable = false
title       = "A Trailing Band Opens a Sub-Band per Tier"

description = '''
`FIRST` and `SECOND` name `base`, and `DERIVED_A` and `DERIVED_B` read that pair, all four sitting below the definition. **A blank line opens between the base pair and the two derived from them**, because a trailing band opens a sub-band per tier the same way a leading band does.
'''

[docs]
previewable = true
title       = "A Custom Allow List Exempts Its Package"

description = '''
`import numpy` and `import torch` are each used through a single attribute access, the shape this lint flags. Naming `numpy` in the `allow` list marks it as a bare import the project keeps on purpose, so only `torch` is reported.
'''

[docs]
previewable = false
title       = "A Dotted Import Inherits Its Top Segment's Exemption"

description = '''
`import numpy.linalg` binds `numpy`, reached here through its `linalg` attribute. The `allow` entry names the top segment, so the dotted import inherits the exemption and passes.
'''

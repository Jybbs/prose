[docs]
previewable = false
title       = "From-Imports Are Outside the Rule's Reach"

description = '''
`from os import path` already names the exact symbol it binds, the explicit member-import shape this lint steers bare imports toward. The rule inspects plain `import` statements and **never a `from … import …`**, so the file carries no diagnostic.
'''

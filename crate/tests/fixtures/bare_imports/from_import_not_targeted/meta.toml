[docs]
previewable = false
title       = "A `from` Import Is Not Reported"

description = '''
`from os import path` already names the exact symbol it binds, the form this lint steers a bare import toward. **No diagnostic is reported**, because the rule reads plain `import` statements only and never a `from … import …`.
'''

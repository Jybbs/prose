[docs]
previewable = false
title       = "An Import Nested in a Function Passes"

description = '''
`import sys` sits inside `show_version`, so its binding is local to the function rather than module-level. This lint inspects only module-scope imports, leaving the function-local import unexamined even though `sys.version` is its single attribute use.
'''

[docs]
previewable = false
title       = "An Import Inside a Function Is Not Reported"

description = '''
`import sys` sits inside `show_version`, and `sys.version` is its only attribute use. **No diagnostic is reported**, because the import binds a function-local name, and the rule measures module-scope imports only.
'''

[docs]
canonical   = true
previewable = true
title       = "A Narrowly-Used Import Flags Where a Wide Fan-Out Passes"

description = '''
`import os` is reached only through `os.environ`, one distinct attribute however many times it repeats, so a `from os import environ` rewrite would cover every use and the bare import is reported. `import torch` fans out across five distinct attributes, past the `max-attributes` budget of 4, so its bare form is **left untouched**.
'''

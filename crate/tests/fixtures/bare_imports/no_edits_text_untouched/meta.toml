[docs]
previewable = true
title       = "Lint Diagnostics Never Rewrite the Source"

description = '''
`os` is reached only through `os.getcwd`, so `bare-imports` reports the import, yet it emits no fix. The source text is left exactly as written, because a lint diagnostic is a report rather than a rewrite.
'''

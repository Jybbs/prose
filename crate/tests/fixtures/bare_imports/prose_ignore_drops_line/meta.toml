[docs]
previewable = false
title       = "A `# prose: ignore` Comment Drops the Diagnostic"

description = '''
`import os  # prose: ignore[bare-imports]` is reached only through `os.getcwd`, the narrow use the lint reports. **No diagnostic is reported**, because the directive drops the finding for that rule on that line alone, and every other rule still reads the line.
'''

[docs]
previewable = false
title       = "A `# prose: ignore` Comment Drops the Diagnostic"

description = '''
`os` is reached through one attribute and would draw the lint, but the `# prose: ignore[bare-imports]` comment on the import line silences it for that rule alone.
'''

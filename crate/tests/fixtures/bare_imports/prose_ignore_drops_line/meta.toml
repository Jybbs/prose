[docs]
previewable = false
title       = "A `# prose: ignore` Comment Drops the Diagnostic"

description = '''
`os` is reached only through `os.getcwd`, the single-attribute shape this lint reports. The `# prose: ignore[bare-imports]` trailing the import line drops the diagnostic for that rule alone, so the import passes while every other rule still reads the line.
'''

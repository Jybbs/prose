[docs]
previewable = true
title       = "Already-Conforming Source Is Left Alone"

description = '''
`import os` and `from sys import argv` arrive already separated by a single blank line, the canonical gap between the bare form and the `from` form. The source **passes through untouched**, staying at one blank.
'''

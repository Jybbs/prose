[docs]
previewable = false
title       = "Class Header Cushions and Oversized Gaps Collapse to One Blank"

description = '''
One blank line opens between `class Posting:` and its first field, and **the three-blank gaps before `m1` and between `m1` and `m2` collapse to a single blank each**. The adjacent pair of fields, `field` and `name`, stays tight with no blank inserted.
'''

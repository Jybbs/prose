[docs]
previewable = false
title       = "A Header Comment Cushions Above, Binds to the Member"

description = '''
The comment `# describes the first method` sits between `class Posting:` and `def m1(self):`, and it reads as `m1`'s leading block. **One blank line opens between the header and the comment**, while the comment stays tight against the method it describes.
'''

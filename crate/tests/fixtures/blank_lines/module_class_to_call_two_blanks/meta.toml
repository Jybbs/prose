[docs]
previewable = false
title       = "A Call after a Module-Level Class Carries Two Blanks"

description = '''
A module-level `class` followed by a bare call normalizes to **two blank lines** between them, regardless of the source's actual count.
'''

[docs]
previewable = false
title       = "A Control-Flow Block after a Def Carries Two Blanks"

description = '''
A module-level `def` followed by a control-flow block normalizes to **two blank lines** between them, regardless of the source's actual count.
'''

[docs]
canonical   = false
previewable = true
title       = "A Flush Column Holds Its Break"

description = '''
An argument list whose opening parenthesis ends its line and whose closing parenthesis opens its own is the deliberate column shape the explode path emits, so it keeps its break whatever the joined form would measure.
'''

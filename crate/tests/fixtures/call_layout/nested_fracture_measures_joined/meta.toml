[docs]
canonical   = false
previewable = true
title       = "An Outer Call Measures Its Nested Fracture Joined"

description = '''
An argument carrying a fractured call of its own measures at that call joined rather than at the truncated width its opening row reads, so the enclosing call reaches the same explode decision on the first pass that it would reach once the nested rejoin lands.
'''

[docs]
previewable = false
title       = "A Parenthesized Value Passes Through Whole"

description = '''
An argument value wrapped in its own grouping parentheses is emitted verbatim, since the rendered keyword ends at the closing parenthesis rather than at the value. The full pipeline sheds the pair first, so the call reshapes there.
'''

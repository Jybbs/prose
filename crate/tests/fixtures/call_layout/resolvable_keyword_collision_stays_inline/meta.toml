[docs]
previewable = false
title       = "A Call Binding One Name Twice Stays Inline"

description = '''
A call whose fourth positional argument binds the same parameter a trailing keyword also names (*`color` here*) cannot be rendered in keyword form without a duplicate key, so it stays inline.
'''

[docs]
previewable = false
title       = "A Sole Generator Argument Takes a Grouping Pair"

description = '''
A bare generator expression is legal as a call's only positional argument and illegal after a `name=` prefix, so binding it to the resolved parameter wraps it in parentheses rather than emitting source that Python rejects.
'''

[docs]
canonical   = false
previewable = true
title       = "An Over-Cap Call the Rule Cannot Name Rejoins"

description = '''
`helper` resolves to no module function, so its arguments never reach `name=value` form and the count trigger passes the call over. Nothing downstream reopens it, so the fracture the author wrote **closes onto one row** rather than holding a shape neither trigger can improve.
'''

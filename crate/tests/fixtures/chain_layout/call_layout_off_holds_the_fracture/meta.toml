[docs]
canonical   = false
previewable = true
title       = "A Cleared `call-layout` Holds the Whole Chain"

description = '''
The chain measure reads each link at the width the fractures inside it settle to, and no fracture closes while [[call-layout]] is off. The link carrying the hand-wrapped argument list therefore still spans lines when the chain is read, so the break declines and the spine stays where the source put it.
'''

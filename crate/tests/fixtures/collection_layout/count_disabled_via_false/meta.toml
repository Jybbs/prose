[docs]
previewable = false
title       = "A False Count Cap Leaves Width as the Only Dict Gate"

description = '''
Setting `max-inline-dict-entries` to `false` disables the count trigger, so a dict that fits the line budget stays inline however many entries it carries.
'''

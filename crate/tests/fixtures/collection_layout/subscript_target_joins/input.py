data[
    key
] += 2

matrix[
    row, column
] = 0

window[
    start:stop
] = []

x = resolved_environment_configuration_table[
    the_currently_active_profiles_index_values
]

y = resolved_environment_configuration_table[
    the_currently_active_profiles_index_valuess
]

result = offset_lookup[
    base_offset
    + computed_delta
]

precedence = coefficient_matrix[
    (row_base + row_delta)
    * column_scale
]

guarded = lookup_table[
    base_offset
    + helper(adjustment)
]

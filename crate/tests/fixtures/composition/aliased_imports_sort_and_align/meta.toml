[docs]
previewable = false
title       = "Aliased Imports Sort, Then Their `as` Keywords Align"

description = '''
Four aliased bare imports arrive out of alphabetical order, with `import numpy as np` first. **`alphabetize-siblings` sorts the statements so `collections` leads, and `align-imports` then pads the `as` keyword of every row into one column** measured against the sorted run.
'''

[docs]
previewable = true
title       = "Aligned Fields Settle Their Comment Column Behind the `=`"

description = '''
**`align-equals` fixes each row's width first, then `align-comments` measures against those settled widths** rather than the ones the author wrote. Both columns land in one pass over the class body, so the values read down one edge and the notes down another.
'''

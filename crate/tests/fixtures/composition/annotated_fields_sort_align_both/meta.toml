[docs]
previewable = false
title       = "Annotated Fields Sort Then Align Both `:` and `=`"

description = '''
The four annotated fields of `Endpoint` sit out of alphabetical order with varied name and default widths. `alphabetize-siblings` reorders them so `backend` leads and `timeout` closes, then **both the type-annotation `:` column and the default `=` column align across the sorted run**.
'''

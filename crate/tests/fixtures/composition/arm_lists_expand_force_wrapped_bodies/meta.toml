[docs]
previewable = true
title       = "Overflowing Arm Lists Expand and Stay Wrapped"

description = '''
Single-statement `match` arms return inline lists that overflow the line budget. `collection-layout` expands each list, and **`align-match-case` leaves those arm bodies wrapped because expansion forces a multi-line statement** the arm cannot collapse.
'''

[docs]
previewable = false
title       = "A Section Marker Holds a Constant Band In Its Own Section"

description = '''
Module-scope constant banding stops at a section marker. A leading constant above a `# --- Imports ---` divider stays in its section while the imports below sort within theirs, so the band never hoists a member across the marker that divides them.
'''

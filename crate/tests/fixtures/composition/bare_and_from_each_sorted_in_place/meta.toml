[docs]
previewable = false
title       = "Bare and From Imports Each Sort Within Their Group"

description = '''
Three aliased bare imports and three `from` imports arrive with each kind out of order. **`alphabetize` sorts each kind in place so `collections` leads both, `align-imports` aligns the `as` and `import` keywords into their columns, and `blank-lines` inserts one blank at the bare-to-from boundary**.
'''

[docs]
previewable = false
title       = "Bare and From Imports Each Sort Within Their Group"

description = '''
Three aliased bare imports and three `from` imports arrive with each kind out of order. **`alphabetize-siblings` sorts each kind in place so `collections` leads both, `align-imports` aligns the `as` and `import` keywords into their columns, and `space-statements` inserts one blank at the bare-to-from boundary**.
'''

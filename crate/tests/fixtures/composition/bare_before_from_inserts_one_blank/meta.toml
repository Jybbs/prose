[docs]
previewable = false
title       = "One Blank Line Is Written Between Adjacent Bare and `from` Imports"

description = '''
`import os` sits directly above `from sys import argv` with no blank line between them. **`space-statements` writes one blank line at the boundary**, because the two statements fall into different import groups and adjacent groups take one blank line between them.
'''

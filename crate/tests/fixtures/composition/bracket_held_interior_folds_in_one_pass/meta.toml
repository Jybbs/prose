[docs]
canonical   = false
previewable = true
title       = "A Pair Whose Breaks a Nested Bracket Holds Joins in One Pass"

description = '''
The grouping pair around the comparison holds none of its breaks, the `find` call's own parentheses do, so [[reflow-parentheses]] folds the interior onto one row and drops the pair in the same pass, leaving [[reflow-calls]] a call already on the row it fits.
'''

[docs]
previewable = true
title       = "A Call Explodes before Its String Run Breaks"

description = '''
`reflow-calls` settles the argument list one entry per line first, so `stack-adjacent-strings` measures the run against the column it lands at and breaks only the argument that still overruns the budget.
'''

[docs]
previewable = true
title       = "A Call Explodes, Then Its Keywords Sort and Align"

description = '''
`reflow-calls` explodes a crowded call to keyword form, `alphabetize-siblings` sorts the keywords, and `align-equals` aligns their `=` on one column, all in the same `format` pass.
'''

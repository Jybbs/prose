[docs]
previewable = true
title       = "A Resolvable Call Explodes, Then Its Keywords Sort"

description = '''
`reflow-calls` runs ahead of `alphabetize-siblings`, so **a crowded call to a module function explodes to keyword form and then its keywords alphabetize in the same pass**. The signature holds its source order, since positional parameters never reorder.
'''

[docs]
canonical   = false
previewable = true
title       = "A Colon Run Measures Past the Delimiter Padding a Later Rule Drops"

description = '''
The stranded space before one key's closing parenthesis goes once [[strip-stranded-padding]] runs, and [[align-colons]] reads each row at that settled width, so the run aligns rather than skipping its column on a breach the padding never reaches.
'''

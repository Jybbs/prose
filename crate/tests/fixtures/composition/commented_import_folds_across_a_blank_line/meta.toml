[docs]
canonical   = false
previewable = true
title       = "A Commented Import Folds Across a Blank Line"

description = '''
[[reflow-imports]] merges across the blank line, so the unread lead is a statement the merge folds into its surviving sibling and [[prune-inert-imports]] gives its line to that sibling, the blank line leaving with the sibling's former line so the comment heads the import it read over.
'''

[docs]
previewable = false
title       = "Dropped Future Import, Then the Gap Collapses to the Cushion"

description = '''
The module opens with `from __future__ import annotations`, and three blank lines separate `CONFIG = {}` from `def add`. **`prune-inert-imports` removes the future import and `blank-lines` collapses the gap to two blank lines**, the standard separation between a module-level constant and the first `def`.
'''

[docs]
previewable = true
title       = "Drop Future, Sort Aliases, Align `as`, Settle Cushion"

description = '''
The module top carries `from __future__ import annotations`, three aliased imports out of order, and three blank lines before `def add`. **The future import is removed, `collections`, `numpy`, and `requests` sort alphabetically, their `as` keywords pad into one column, and the gap collapses to two blank lines** before the function.
'''

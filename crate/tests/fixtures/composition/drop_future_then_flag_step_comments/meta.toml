[docs]
previewable = false
title       = "Drop Future Import, Then Flag Step-Narration Comments"

description = '''
A `from __future__ import annotations` precedes step-narration comments in a function body. `prune-inert-imports` removes the import, and **`step-narration` emits one diagnostic per offending comment whose range tracks the post-removal offset**.
'''

[docs]
previewable = false
title       = "A Comment Between Import Groups Stays With the `from` Below It"

description = '''
The comment `# boundary` sits between `import os` and `from sys import argv`, two imports in different groups. **The blank line between the groups is written above the comment**, so `# boundary` stays tight against the `from sys import argv` line it heads.
'''

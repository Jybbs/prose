[docs]
previewable = true
title       = "Grouping Off Leaves One Flat, Aligned Import Block"

description = '''
With `group-imports = false`, `alphabetize` sorts the imports as **one flat block**, `blank-lines` adds no section dividers, and `align-imports` still aligns the `import` keyword across the `from` lines.
'''

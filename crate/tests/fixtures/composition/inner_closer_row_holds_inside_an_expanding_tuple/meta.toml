[docs]
previewable = true
title       = "An Inner Closer Row Holds Inside an Expanding Tuple"

description = '''
The tuple closing the `%` operand overflows, so `reflow-collections` expands it and `reflow-calls` leaves the `type` call inside alone, where the tail the expansion removes would otherwise have exploded it.
'''

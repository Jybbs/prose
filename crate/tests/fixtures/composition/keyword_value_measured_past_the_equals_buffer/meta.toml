[docs]
canonical   = false
previewable = true
title       = "An Exploded Keyword Value Measures Past the `=` Buffer"

description = '''
[[reflow-calls]] lands every keyword alone on its row, where [[align-equals]] then seats a buffer around each `=` and moves the value two columns right. Measuring the nested call from that settled column is what explodes it in this pass rather than leaving an over-budget row for the next one.
'''

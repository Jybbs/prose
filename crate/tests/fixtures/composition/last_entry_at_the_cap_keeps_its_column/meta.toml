[docs]
canonical   = false
previewable = true
title       = "A Last Entry Aligned onto the Cap Keeps Its Column"

description = '''
The dict is already in the order [[alphabetize-siblings]] leaves it, so its last entry gains no separator and [[reflow-collections]] measures the row [[align-colons]] pads onto the cap at exactly the cap. The column holds rather than stripping on the next pass.
'''

[docs]
previewable = true
title       = "Methods Sort Around One Opening With `try`/`except`"

description = '''
Out-of-order methods, one opening its body with a `try`/`except` block. **`alphabetize-siblings` reorders the methods and `space-statements` cushions them**, leaving the compound-statement body of the reordered method untouched.
'''

[docs]
previewable = false
title       = "A Signature Draws the Report and the Strip Together"

description = '''
`emit` declares `-> None` and takes the unannotated parameter `event`. `strip-none-return` removes the redundant return annotation, and `signature-annotations` reports the missing annotation on `event`, so the one `def` picks up both the rewrite and the diagnostic.
'''

[docs]
previewable = false
title       = "One Signature Takes Both the Report and the Strip"

description = '''
`emit` declares `-> None` and takes the unannotated parameter `event`. **`strip-none-return` removes the return annotation and `signature-annotations` reports the missing annotation on `event`**, so the one `def` takes both the rewrite and the diagnostic.
'''

[docs]
canonical   = false
previewable = true
title       = "A Relocated Block Remeasures the Calls Inside It"

description = '''
[[reflow-calls]] explodes `tuple` at the indent the row holds before the tuple around it expands, and [[reflow-collections]] moves that block one step deeper and measures the `_mapdict_values` call at the column it lands at, exploding it there.
'''

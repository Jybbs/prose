[docs]
previewable = true
title       = "Short Dict Keys Sort In Place Without Expanding"

description = '''
A short inline dict with out-of-order keys that fits the line budget. **`alphabetize-siblings` reorders the keys in place and the dict stays inline**, because the fitting width gives `reflow-collections` no reason to expand.
'''

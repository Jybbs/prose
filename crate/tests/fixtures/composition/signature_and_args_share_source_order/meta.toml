[docs]
previewable = true
title       = "Signature and Args Share One Source Order"

description = '''
The signature's parameters `template`, `context_map`, and `escape_html` already list in the same order as the docstring's `Args:` entries below. **`alphabetize-siblings` leaves that order untouched because `Args:` entries mirror the signature rather than sorting alphabetically, `align-colons` aligns the three entries' `:` column, and `wrap-docstrings` wraps each description** with hanging indent under that aligned column.
'''

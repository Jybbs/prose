[docs]
previewable = true
title       = "A Signature Measures the Stripped Padding"

description = '''
`reflow-signatures` measures the one-line form at the width it settles to once `strip-stranded-padding` drops the colon padding inside a parameter, so a signature that fits only after the strip stays on one line rather than exploding and re-joining.
'''

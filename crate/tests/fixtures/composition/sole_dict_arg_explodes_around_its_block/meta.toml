[docs]
previewable = true
title       = "A Sole Dict Argument Explodes the Call Around It"

description = '''
A dict handed to a call as its only argument spans rows once expanded, so the call explodes around it rather than opening the dict on the calling row and closing both brackets together. The span trigger reads the row break whatever the argument count and whatever the joined width would have been.
'''

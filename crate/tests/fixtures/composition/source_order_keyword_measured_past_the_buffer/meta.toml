[docs]
canonical   = false
previewable = true
title       = "A Source-Order Keyword Measures Its Buffer and Its Separator"

description = '''
The method call cannot take keyword form, so it explodes in source order, and each keyword value still measures from the column the [[align-equals]] buffer settles it at and charges the comma closing its row, so the `outline` value explodes.
'''

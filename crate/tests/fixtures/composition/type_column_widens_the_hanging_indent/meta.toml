[docs]
previewable = false
title       = "A Widened Head Carries Its Continuation Column With It"

description = '''
`align-colons` seats the type-group column and the `:` column, then `docstring-wrap` reflows the overflowing description from the head the padding leaves, so the continuation hangs under the description rather than under the column the source wrote.
'''

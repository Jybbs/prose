[docs]
canonical   = false
previewable = false
title       = "An Over-Cap Member the Rule Cannot Name Rejoins Inside Its Literal"

description = '''
The member runs past `max-args` against a callee resolving nowhere, so `call-layout` closes its break rather than exploding it, and `collection-layout` then reads a literal whose entries all sit on one row and leaves it there.
'''

[docs]
previewable = false
title       = "Lone Key Leaves Every Other Field at Its Default"

description = '''
The snippet sets `target-version = "3.10"` under `[tool.prose]` and nothing else. **Every other field resolves to its built-in default**, so the loaded `Config` pairs the 3.10 target with the stock line lengths, the enabled cache, and every rule toggle at `true`.
'''

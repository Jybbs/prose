[docs]
previewable = true
title       = "Comment-Led Code Examples Pass Through Verbatim"

description = '''
The example under `Summary line.` opens with a `#` comment and continues into `total = sum(values)` and `print(total)`. **The comment opens a passthrough block running to the next blank line**, so each statement keeps its own line instead of merging into a single run-on paragraph.
'''

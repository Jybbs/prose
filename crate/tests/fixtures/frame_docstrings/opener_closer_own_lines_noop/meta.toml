[docs]
previewable = true
title       = "Already-Canonical Multi-Line Docstrings Are Left Alone"

description = '''
`greet`'s multi-line docstring already holds its opening and closing `"""` on their own lines, so the output is **left unchanged**. The rule fires only when one of the two boundaries shares a line with body content, and neither does here.
'''

[docs]
previewable = true
title       = "Triple-Single-Quoted Docstrings Convert to Triple Double"

description = '''
`summarize`'s docstring sits between triple-single-quote delimiters. **It converts to the canonical `"""` form** PEP 257 sanctions, and the body lines `Summary line.` and `More detail here.` stay exactly where they were, leaving the delimiters as the only change.
'''

[docs]
previewable = false
title       = "An Aliased `__future__` Import Still Leads"

description = '''
The directive carries an `as` alias, which the classifier never reads, so the statement holds the front while the bare and external imports below it partition as usual.
'''

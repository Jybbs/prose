[docs]
previewable = true
title       = "An Already-Grouped Run Passes Through Untouched"

description = '''
With `myapp` named in `first-party`, the run already sits in bare → external `from` → first-party order, so the rule **emits no edit**. The partition rewrites only when it would relocate an import, and every statement here already sits in its group.
'''

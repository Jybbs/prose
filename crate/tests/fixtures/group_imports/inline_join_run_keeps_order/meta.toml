[docs]
previewable = false
title       = "A Semicolon-Joined Run Keeps Source Order"

description = '''
`from collections import abc` and `import sys` share one physical line, joined by a `;`. The partition relocates whole one-per-line statements, and neither of these owns its line, so the rule **leaves the pair untouched** in source order.
'''

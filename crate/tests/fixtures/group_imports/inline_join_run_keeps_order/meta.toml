[docs]
previewable = false
title       = "A Semicolon-Joined Pair Keeps Its Source Order"

description = '''
`from collections import abc` and `import sys` share one physical line, joined by `;`. **`group-imports` leaves the pair as written**, because the rule moves whole statements that own their line, and neither of these does.
'''

[docs]
previewable = true
title       = "Imports Inside a Function Body Partition Too"

description = '''
The rule reaches into every nested body, so the run opening `load` partitions exactly as a module run would. **The rule groups the pair in place**, lifting the bare `import sys` above `from collections import Counter` while both keep the function-body indent.
'''

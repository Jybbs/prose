[docs]
previewable = true
title       = "Imports Inside a `TYPE_CHECKING` Arm Partition Too"

description = '''
The rule reaches into compound-statement arms, so the run under `if TYPE_CHECKING:` **groups in place** exactly as a module-level run would. The bare `import sys` moves above `from collections import abc`, and both lines keep the arm's indent.
'''

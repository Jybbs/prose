[docs]
previewable = false
title       = "Empty Pipeline Returns Source Unchanged"

description = '''
The single binding `x = 1` runs through a pipeline with **no rules enabled** and comes out byte-for-byte identical, with no diagnostics. The case confirms the empty pipeline is the identity transform that touches nothing.
'''

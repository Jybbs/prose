[docs]
previewable = false
title       = "An Empty Pipeline Returns the Source Unchanged"

description = '''
The single assignment `x = 1` runs through a pipeline with no rules enabled. **The output is byte-for-byte identical to the input and no diagnostic is emitted**, which pins the empty pipeline as the identity transform.
'''

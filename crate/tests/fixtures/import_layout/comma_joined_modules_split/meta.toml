[docs]
previewable = true
title       = "A Comma-Joined Import Splits One Module per Line"

description = '''
`split-multi-module` breaks `import a, b` into **one `import` statement per module**, the shape [[group-imports]] and [[alphabetize]] then order. The commas here separate distinct modules, so nothing binds them to one line.
'''

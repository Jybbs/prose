[docs]
previewable = false
title       = "A Function-Body Import Splits at Its Own Indent"

description = '''
The split reads the indent of the line it rewrites, so a deferred import inside a function body opens every new statement at the **body's own column**.
'''

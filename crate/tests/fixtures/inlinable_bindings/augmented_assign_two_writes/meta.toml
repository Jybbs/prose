[docs]
previewable = true
title       = "Augmented Assignments Count as Both Write and Read"

description = '''
`total = 0` is the first write, and `total += value` inside the loop is **both a read and a write** of `total`, lifting the write count above one. A binding written twice cannot dissolve into a single use site, so the accumulator is not flagged.
'''

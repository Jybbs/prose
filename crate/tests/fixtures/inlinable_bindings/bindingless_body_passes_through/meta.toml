[docs]
previewable = false
title       = "A Function With No Bindings Passes Through Unchanged"

description = '''
`main` binds no name, so there is nothing for the rule to count. **The run emits no diagnostic and no edit**, and the file passes through exactly as written, since the rule reports and never rewrites.
'''

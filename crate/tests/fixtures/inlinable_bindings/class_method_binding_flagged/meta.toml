[docs]
previewable = false
title       = "A Binding Inside a Method Is Reported"

description = '''
`normalized` inside `Service.consume` is written once from `transform(payload)` and read once in `return normalized`. **It is reported the same way a binding in a plain function is**, because the rule reads into class bodies and counts each method's scope on its own, so a method is not exempt.
'''

[docs]
previewable = false
title       = "Bindings Inside Class Methods Are Flagged"

description = '''
`normalized` inside `Service.consume` **is flagged** the same as any once-read binding in a plain function, because `inlinable-bindings` walks into class bodies and evaluates each method's scope on its own. Methods carry no blanket exemption from the rule.
'''

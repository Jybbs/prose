[docs]
previewable = true
title       = "A Binding a `del` Names Spares the Target"

description = '''
`handle` is written once and read once, and a later `del handle` names it again without counting as a read. **The binding stays unreported**, because standing `acquire()` in at the read would leave `del handle` naming a value nothing bound.
'''

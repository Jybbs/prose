[docs]
previewable = true
title       = "A Value Spanning Rows Resolves No Inline"

description = '''
`totals` is written once and read once, yet its dict literal already spans rows. **The binding stays unreported**, because the replacement would carry those rows into the `return` and grow the file for a name removed.
'''

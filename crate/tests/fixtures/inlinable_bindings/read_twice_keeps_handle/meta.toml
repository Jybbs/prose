[docs]
previewable = false
title       = "A Binding Read Twice Is Not Reported"

description = '''
`x` in `doubled` is written once from `compute()` and read twice in `return x + x`. **The binding is not reported**, because inlining would either run `compute()` twice or repeat the expression, so the named binding stays.
'''

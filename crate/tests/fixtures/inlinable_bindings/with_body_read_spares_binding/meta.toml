[docs]
previewable = false
title       = "A Read Inside a `with` Body Is Not Reported"

description = '''
`encoded` is written above the `with` from `payload.encode()` and read once inside its body, so inlining would run `payload.encode()` with the file already open. **The binding is not reported**, the same outcome a read in a `try` arm gets.
'''

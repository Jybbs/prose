[docs]
previewable = false
title       = "An Import Line Answers to the Import Budget"

description = '''
The single-name import runs past `code-line-length` but stays within `import-line-length`, and a line inside an import statement answers to the import budget, so `line-overflow` leaves it unflagged.
'''

[docs]
previewable = false
title       = "A Call Inside a Replacement Field Still Reports"

description = '''
The over-cap line carries an inline call, yet the call sits inside an f-string replacement field where no layout rule reaches it, so the line has no legal reshape left and surfaces as a finding rather than deferring to `reflow-calls`.
'''

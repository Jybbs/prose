[docs]
previewable = false
title       = "A Reshapeable Line Stays Quiet"

description = '''
The over-cap line carries an inline call `reflow-calls` could still explode, so `line-overflow` leaves it for that rule and emits nothing. Only the remainder no split can shorten surfaces as a finding.
'''

[docs]
previewable = false
title       = "A Reshapeable Collection Stays Quiet"

description = '''
The 44-column `data` assignment runs over the 40-column budget, yet `line-overflow` emits nothing. The three-entry dict is a collection `reflow-collections` can still explode one entry per line, so the overflow is left to that rule.
'''

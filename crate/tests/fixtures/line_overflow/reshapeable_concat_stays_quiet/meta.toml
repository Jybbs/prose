[docs]
previewable = false
title       = "An Implicit Concatenation Stays Quiet"

description = '''
A plain string and an f-string join as one implicitly concatenated run, which `string-concat-layout` breaks one literal per line, so the over-cap line is left for that rule rather than surfacing as a finding.
'''

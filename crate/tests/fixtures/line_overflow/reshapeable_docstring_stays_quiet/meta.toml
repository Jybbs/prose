[docs]
previewable = false
title       = "A Docstring Line Is Not Reported"

description = '''
Both prose lines of the module docstring run past the 40-column budget. **`line-overflow` emits nothing**, because a line inside a docstring is text `wrap-docstrings` can reflow to the budget, so the overflow is left to that rule.
'''

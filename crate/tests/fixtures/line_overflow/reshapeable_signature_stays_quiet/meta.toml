[docs]
previewable = false
title       = "A Reshapeable Signature Stays Quiet"

description = '''
The over-cap signature carries parameters `reflow-signatures` could still explode one per line, so `line-overflow` leaves it for that rule and emits nothing.
'''

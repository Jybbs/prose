[docs]
previewable = false
title       = "A Reshapeable Signature Stays Quiet"

description = '''
`def configure(first, second, third, fourth):` runs to 44 columns, over the 40-column budget, yet `line-overflow` emits nothing. The signature carries parameters `signature-layout` can still explode one per line, so the overflow is left to that rule.
'''

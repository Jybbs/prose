[docs]
previewable = true
title       = "Turning the Facet Off Leaves the Bare Report"

description = '''
With `suggest-string-splits = false`, the finding on `BANNER`'s 54-column line carries no suggested split, only the report naming the line and its budget. The message still omits `with no legal reshape`, because the split of this string exists whether or not the finding spells it out.
'''

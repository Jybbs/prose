[docs]
previewable = true
title       = "An Annotated Module Constant Is Flagged Too"

description = '''
`timeout: int = 30` binds a module constant just as an unannotated assignment would, so the annotation changes nothing and the lint flags it with the suggested `TIMEOUT` rename. Only a bare annotation with no value binds nothing and goes unflagged.
'''

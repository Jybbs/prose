[docs]
previewable = true
title       = "A Bare Annotation Binds No Value"

description = '''
`max_retries: int` declares a type without binding a value, so no constant exists for the lint to classify. The rule reports only assignments that bind one, leaving this line unflagged.
'''

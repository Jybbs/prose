[docs]
previewable = false
title       = "A Lambda Value Is Exempt"

description = '''
`to_key = lambda row: row.id` binds a callable rather than data, the same standing a `def` gives a name. **The lint never suggests a `TO_KEY` rename**, because a function is not a constant.
'''

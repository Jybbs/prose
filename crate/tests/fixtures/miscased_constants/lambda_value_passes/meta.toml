[docs]
previewable = false
title       = "A Lambda Value Is Spared"

description = '''
`to_key = lambda row: row.id` binds a callable rather than data, the same standing a `def` would give the name. A function is not a constant, so the lint never suggests a `TO_KEY` rename.
'''

[docs]
previewable = true
title       = "A Reassigned Name Behaves Like a Variable"

description = '''
`counter` is assigned twice at module level, making it mutable state rather than a constant, so its lowercase name is exactly right and the lint passes over it. The mirror case belongs to [[reassigned-constants]], which flags a `SCREAMING_CASE` name that a later write contradicts.
'''

[docs]
previewable = true
title       = "A SCREAMING_CASE Constant Stays Silent"

description = '''
`MAX_RETRIES = 5` assigns a module-level constant whose name is already SCREAMING_CASE, so the lint reports nothing. This is the shape every flagged case suggests renaming to.
'''

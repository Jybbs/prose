[docs]
previewable = false
title       = "An Import Holds While Any Read Survives"

description = '''
One annotation rewrites and one holds, so `Optional` is still read once and its import stays.
'''

[docs]
previewable = true
title       = "A Generic Whose Target Needs an Import Holds"

description = '''
`Deque` maps to `collections.deque` rather than to a builtin, and the rewrite adds no import, so
the annotation stays as written.
'''

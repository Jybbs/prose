[docs]
previewable = false
title       = "A Comment Inside the Subscript Keeps the Legacy Form"

description = '''
`x`'s `Optional[` subscript spans three lines with the `# the count` comment inside its brackets. **The annotation stays in its legacy form**, because rewriting it to `int | None` would rebuild the expression and drop that comment.
'''

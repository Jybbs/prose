[docs]
previewable = true
title       = "A Forward-Reference Member Holds the Union"

description = '''
`Optional["Node"]` wraps the forward-reference string `"Node"`, and the modern spelling `"Node" | None` raises when the annotation is evaluated, because `str` defines no `|` operator. The whole annotation therefore holds in its legacy form.
'''

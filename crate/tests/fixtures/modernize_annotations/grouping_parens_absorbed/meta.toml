[docs]
previewable = false
title       = "Grouping Parentheses Are Absorbed Into the Rewrite"

description = '''
`x`'s annotation is `(Optional[int])`, wrapped in grouping parentheses. **`int | None` is written bare rather than re-wrapped**, because the rewrite replaces the whole parenthesized span, and the unread `Optional` import is removed.
'''

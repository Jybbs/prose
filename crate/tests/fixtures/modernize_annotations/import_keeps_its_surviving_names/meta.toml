[docs]
previewable = true
title       = "Only the Names the Rewrite Read Are Dropped"

description = '''
`Optional[int]` rewrites to `int | None`, leaving `Optional` unread, whereas the `cast(object, value)` call still reads `cast` at runtime. Only `Optional` is dropped from the import, which survives as `from typing import cast`.
'''

[docs]
previewable = false
title       = "A Legacy Member Inside a Modern Generic Rewrites in Place"

description = '''
`table: dict[str, Optional[int]]` already uses the builtin `dict` spelling around a legacy `Optional[int]` member. **Only the member rewrites, so the annotation ends up as `dict[str, int | None]`**, and with `Optional`'s only read gone its import is removed as well.
'''

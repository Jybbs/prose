[docs]
previewable = false
title       = "A Legacy Member Inside a Modern Generic Rewrites in Place"

description = '''
`table`'s enclosing `dict[...]` is already the modern builtin spelling, so only the `Optional[int]` member inside it rewrites, landing the annotation on `dict[str, int | None]`. That was `Optional`'s only read, so its import is removed as well.
'''

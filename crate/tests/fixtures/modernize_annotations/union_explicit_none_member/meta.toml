[docs]
previewable = false
title       = "An Explicit `None` Member Joins the Pipe Run"

description = '''
`x: Union[int, str, None]` names `None` outright as its last member. **The three members join with `|` as `int | str | None`, with no extra `| None` appended**, and the `Union` import is removed once nothing reads it.
'''

[docs]
previewable = true
title       = "A Comment Trailing an Open Bracket Takes the Floor Too"

description = '''
The two-column floor applies wherever a comment trails code, an open bracket included. [[strip-stranded-padding]] declines the padding in that exact position so the comment never fuses onto the bracket, leaving this rule to settle how wide the gap reads.
'''

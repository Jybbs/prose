[docs]
canonical   = true
previewable = true
title       = "Every Comment Opens on a Single `# ` and Clears Two Spaces of Code"

description = '''
The trailing `#note` gains the space its text was missing and moves two columns clear of `x = 1`, while the full-line `#tidy this` below opens the same way. Both land on the single `# ` shape, so the eye reads the delimiter once rather than re-parsing it per line.
'''

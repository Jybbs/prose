[docs]
previewable = false
title       = "Comments Already Reading Right Emit Nothing"

description = '''
The full-line comment already opens on `# `, and the comment trailing `value = 1` already sits two columns clear of its code, so both are settled as written. The rule emits no edit and the file passes through byte-identical.
'''

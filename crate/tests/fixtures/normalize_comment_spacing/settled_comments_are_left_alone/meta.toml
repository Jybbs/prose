[docs]
previewable = false
title       = "Comments Already Spaced Right Emit Nothing"

description = '''
A full-line comment already opens on `# `, and the comment trailing `value = 1` already sits two columns clear of its code. **The rule emits no edit**, because both comments already carry the spacing it writes, so the file is left as written.
'''

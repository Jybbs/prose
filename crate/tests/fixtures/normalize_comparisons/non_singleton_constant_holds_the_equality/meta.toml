[docs]
previewable = false
title       = "An Equality Against a Non-Singleton Constant Holds"

description = '''
`None == 0` compares `None` against `0`, and `0` is not a singleton, so rewriting the `==` to `is` would test whether the two are the same object, something the interpreter never guarantees for an integer. The equality therefore holds as written.
'''

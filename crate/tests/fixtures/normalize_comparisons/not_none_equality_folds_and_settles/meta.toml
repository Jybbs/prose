[docs]
previewable = true
title       = "A Negated None Equality Settles in One Pass"

description = '''
`not value == None` stacks two rewrites, in that the `== None` should read as an identity test and the leading `not` should fold into the operator. Both land together, so the condition comes back as `value is not None` rather than passing through an intermediate form.
'''

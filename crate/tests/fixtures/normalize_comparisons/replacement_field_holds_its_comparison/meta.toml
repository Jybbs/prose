[docs]
previewable = false
title       = "A Comparison Inside a Replacement Field Holds"

description = '''
The `x == None` inside `label`'s f-string and the `not k in table` inside `other`'s both hold as written. An f-string replacement field is opaque to this rule, the same way it is to the layout rules, so a comparison written inside one keeps the shape its author gave it.
'''

[docs]
canonical   = true
previewable = true
title       = "Every String Settles on the Double Quote"

description = '''
`'ada'`, `'hello, world'`, and the empty `''` each hold neither quote character in their body, so all three swap to `"`. **The escape count ties at zero and the tie goes to the double quote**, so the module reads one delimiter throughout.
'''

[docs]
previewable = false
title       = "A Triple-Quoted Body Abutting the Closer Stands"

description = '''
`sentinel` holds a `"""` run in its body and `trailing` ends in a bare `"`, and either one would collide with the `"""` closer a swap would install. Both keep their triple-single delimiters rather than corrupt the literal.
'''

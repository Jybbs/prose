[docs]
previewable = false
title       = "A Cell Drops Its `__future__` Directive"

description = '''
The first cell opens on `from __future__ import annotations` above `value = 1`, code holding no annotation the directive would change. **The directive drops inside its own cell**, and the deletion stops short of the boundary between that cell and the `total = value + 1` one after it.
'''

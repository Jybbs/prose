[docs]
previewable = false
title       = "A Cell Empties When Every Import It Holds Goes"

description = '''
The first cell holds nothing but `import json` and `import os`, and no cell in the notebook references either module. **Both imports drop and the cell empties**, keeping the boundary before the `value = 1` cell rather than merging the two.
'''

[docs]
previewable = false
title       = "Assignments Align Inside a Code Cell"

description = '''
The code cell assigns `x = 1` above `yy = 2` under the `# Demo` markdown cell. **The pair aligns on the `=` column**, and the markdown cell and the saved output of the code cell pass through untouched.
'''

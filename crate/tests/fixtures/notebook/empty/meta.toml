[docs]
previewable = false
title       = "A Notebook With No Cells Passes Unchanged"

description = '''
The `cells` array of the notebook is empty, so no code reaches the pipeline. **The file passes through unchanged**, and the run emits nothing.
'''

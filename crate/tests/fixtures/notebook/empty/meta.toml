[docs]
previewable = false
title       = "Format a Notebook With No Code Cells"

description = '''
This notebook's `cells` array is empty, so no code reaches the pipeline at all. Formatting it is **a clean no-op**.
'''

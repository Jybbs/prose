[docs]
previewable = false
title       = "Band a Lambda Constant Within a Cell"

description = '''
`KEY = lambda row: row.score` sits below `helper` in its cell, and a `lambda` value is inert, because its body runs only when the lambda is later called rather than when the assignment binds. The constant therefore hoists into the leading band above the definition, all inside the one cell.
'''

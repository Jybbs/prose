[docs]
previewable = false
title       = "A `lambda` Constant Bands Within a Cell"

description = '''
`KEY = lambda row: row.score` sits below `helper` in its cell. **The constant moves into the leading band above the definition, inside the one cell**, because a `lambda` value is inert, its body running only when the lambda is later called rather than when the assignment binds.
'''

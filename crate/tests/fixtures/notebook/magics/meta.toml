[docs]
previewable = false
title       = "IPython Magics Are Stepped Over"

description = '''
The cell opens with the line magic `%matplotlib inline` and the shell escape `!pip install numpy`, syntax that parses only in IPython mode, above `x = 1` and `yy = 2`. **The two magic lines pass through as written and the assignments below them align on the `=` column.**
'''

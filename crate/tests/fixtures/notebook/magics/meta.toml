[docs]
previewable = false
title       = "Step Over IPython Magics"

description = '''
The cell opens with the line magic `%matplotlib inline` and the shell escape `!pip install numpy`, syntax that parses only in IPython mode. **The rules step over both magics and align the `x` and `yy` assignments below them** on the `=` column.
'''

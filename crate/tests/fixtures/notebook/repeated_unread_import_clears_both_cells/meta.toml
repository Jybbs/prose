[docs]
previewable = false
title       = "A Repeated Unread Import Is Removed From Both Cells"

description = '''
Each cell opens with `import os`, and neither cell reads the name. **Both `import os` lines are removed**, leaving each cell with only its own assignment, because the cells concatenate into one module before the rules read it, so the second import counts as a repeat of the first.
'''

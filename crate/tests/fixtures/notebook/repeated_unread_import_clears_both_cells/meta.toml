[docs]
previewable = false
title       = "A Repeated Unread Import Clears Both Cells"

description = '''
Each cell opens with `import os` and neither cell reads the name.

Cells concatenate into one module before the walk, so the second `import os` reads as a repeat of the first. **Both lines go together**, leaving each cell holding only its own assignment.
'''

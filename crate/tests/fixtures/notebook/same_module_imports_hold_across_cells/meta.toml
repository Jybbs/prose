[docs]
previewable = false
title       = "Same-Module Imports in Two Cells Hold Their Own Statements"

description = '''
Folding one cell's `from pkg import ...` into another's would move code across a cell boundary, so [[reflow-imports]] **gathers only within a single cell** and leaves the two statements where their author put them.
'''

[docs]
previewable = false
title       = "A Bytes Conversion Stays as Written"

description = '''
`raw = "%b" % (payload,)` uses `%b`, a conversion defined for `bytes` templates rather than `str` ones. **The line stays as written**, because no f-string field spells `%b` and the rewrite has nothing to convert it into.
'''

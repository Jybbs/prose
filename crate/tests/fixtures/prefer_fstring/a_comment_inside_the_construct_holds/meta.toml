[docs]
previewable = true
title       = "A Comment Inside the Construct Holds"

description = '''
The `# the note stays` comment sits inside `noted`'s `%` construct, and an f-string has no place to put a comment. The template is left as written rather than converted at the cost of dropping the note.
'''

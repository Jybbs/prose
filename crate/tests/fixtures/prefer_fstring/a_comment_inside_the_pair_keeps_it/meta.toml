[docs]
previewable = false
title       = "A Comment Inside the Pair Keeps It"

description = '''
The `# the note stays` comment hangs on the parentheses wrapping the template rather than sitting inside the `%` construct itself, so nothing blocks the conversion. `"%s" % (value,)` becomes `f"{value}"`, and the wrapping pair stays with its comment in place.
'''

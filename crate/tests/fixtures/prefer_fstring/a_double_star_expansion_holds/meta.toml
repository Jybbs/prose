[docs]
previewable = true
title       = "A Double-Star Expansion Holds"

description = '''
`{key}` is filled through the `**table` expansion, so no argument in the `.format` call names the value the field reads. With nothing to substitute into the field, the template is left as written.
'''

[docs]
previewable = true
title       = "A Lambda Value Holds"

description = '''
Splicing `lambda: 1` into an f-string field would put the lambda's `:` where the field grammar reads the start of a format spec. The template `"%s" % (lambda: 1,)` is left as written rather than converted into a field that parses differently.
'''

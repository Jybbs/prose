[docs]
previewable = true
title       = "A Rewrite Past The Line Budget Holds"

description = '''
No layout rule reaches inside an f-string, so a conversion that would push
its line past `code-line-length` is left as the wrapped form the layout
rules can still reach.
'''

[docs]
previewable = false
title       = "A Format Spec Carrying Its Own Field Holds"

description = '''
The spec in `"{:{width}}"` nests a second replacement field, wherein the outer slot reads `value` and the inner one reads the keyword argument `width=size`. A format spec that carries its own field is a shape `prefer-fstring` leaves alone, so the call holds.
'''

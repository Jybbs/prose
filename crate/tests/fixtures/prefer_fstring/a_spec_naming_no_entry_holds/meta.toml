[docs]
previewable = false
title       = "A Spec Naming No Dict Entry Keeps the Template"

description = '''
The spec `%(missing)s` names a key the dict never defines, `"other"` being its only entry. **The line stays as written**, so it still raises `KeyError` at runtime, because no expression exists for the rewrite to substitute for the absent value.
'''

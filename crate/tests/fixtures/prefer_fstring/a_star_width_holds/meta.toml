[docs]
previewable = false
title       = "A `*` Width Keeps the Template"

description = '''
The `*` in `"%*f"` reads its field width from the argument tuple, so `width` is consumed as the width rather than as the value the `f` conversion renders. **The line stays as written**, because a single replacement field has no slot for that extra argument.
'''

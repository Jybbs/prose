[docs]
previewable = true
title       = "A Starred Argument Holds"

description = '''
`*pair` expands at runtime into however many arguments `pair` holds, so neither `{}` field maps to an expression visible in the source. With no per-field value to inline, the call holds.
'''

[docs]
previewable = true
title       = "A Skipped Line Keeps Its Template"

description = '''
The `# prose: skip` directive holds the line it sits on, so `held` keeps its `"%s" % (value,)` template untouched. The suppression reaches no further than its own line, leaving the neighbour below to convert to `f"{other}"`.
'''

[docs]
previewable = false
title       = "A Template Stating No Field Holds"

description = '''
Neither template carries a substitution, in that `"no fields here"` holds no `{}` field and `"no specs here"` holds no `%` spec. Converting would not move `value` or the empty tuple inline, it would delete them outright, so both lines hold.
'''

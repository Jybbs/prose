[docs]
previewable = false
title       = "A Template With No Field Keeps Its Call"

description = '''
Neither template writes a substitution, in that `"no fields here"` has no `{}` field and `"no specs here"` has no `%` spec. **Both lines stay as written**, because converting would delete `value` and the empty tuple outright rather than moving them inline.
'''

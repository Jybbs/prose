[docs]
previewable = false
title       = "A `u` Prefix Holds"

description = '''
An f-string carries no `u` marker, so the template holds. `normalize-literals`
drops the prefix earlier in the pipeline, leaving this the shape a project
reaches with `unify-prefixes` off.
'''

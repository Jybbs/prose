[docs]
previewable = true
title       = "A Value Carrying a Quote Holds"

description = '''
Moving `table["key"]` into the template would put its `"` quotes inside an f-string delimited by the same character, where the first inner quote would close the string early. The value stays outside the template, so the line holds.
'''

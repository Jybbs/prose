[docs]
previewable = false
title       = "A Value Spanning Two Lines Keeps the Template"

description = '''
The tuple's one member, `first + second`, is written across two lines. **The statement stays as written**, because a replacement field cannot contain a line break below the PEP 701 floor and the `target-version = "3.10"` config sits below it.
'''

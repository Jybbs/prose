[docs]
previewable = true
title       = "A Bare Right-Hand Side Holds"

description = '''
`value` may hold a one-element tuple, which `%` unpacks and a replacement
field does not, so the two forms would render differently.
'''

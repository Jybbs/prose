[docs]
previewable = false
title       = "A Spec Count Past the Tuple Length Keeps the Template"

description = '''
The template writes two `%s` specs while the tuple supplies only `only`, a mismatch that raises `TypeError` at runtime. **The template stays as written**, because the second spec has no expression for the rewrite to move inline.
'''

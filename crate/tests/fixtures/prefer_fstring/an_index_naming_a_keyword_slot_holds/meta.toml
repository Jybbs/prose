[docs]
previewable = false
title       = "An Index Naming a Keyword Slot Holds"

description = '''
The field `{1}` names the second positional slot, but the call passes only `first` positionally, `second=other` arriving by keyword. Positional numbering reaches the positional arguments alone, so the index resolves to nothing and the call holds as written.
'''

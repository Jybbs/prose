[docs]
previewable = false
title       = "An Argument No Field Reads Keeps the Call"

description = '''
The single `{}` field reads `used`, leaving `spare` an argument no field consumes. **The line stays as written**, because the call still evaluates `spare` and a rewrite to `f"{used}"` would drop that evaluation.
'''

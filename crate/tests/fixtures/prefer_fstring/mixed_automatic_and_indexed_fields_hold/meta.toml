[docs]
previewable = false
title       = "A Template Mixing Automatic and Indexed Fields Keeps Its Call"

description = '''
Each template mixes an automatic `{}` with an indexed `{0}`, a combination `str.format` rejects with `ValueError` whichever comes first. **Both calls stay as written**, because each raises at runtime and the rewrite has no valid binding to write.
'''

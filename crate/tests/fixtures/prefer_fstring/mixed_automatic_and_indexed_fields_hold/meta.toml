[docs]
previewable = false
title       = "A Template Mixing Automatic and Indexed Fields Holds"

description = '''
Each template mixes an automatic `{}` with an indexed `{0}`, a combination `str.format` rejects with `ValueError` no matter which comes first. Both calls raise at runtime as written, so neither converts and both lines hold.
'''

[docs]
previewable = false
title       = "References Defined Before Their Annotation Resolve Eagerly"

description = '''
Every annotation in the file names `Node`, and the `class Node` definition sits above the first annotation that reads it. **Eager evaluation would succeed even on Python 3.13**, where annotations evaluate at definition time, so the `from __future__ import annotations` directive changes nothing and is removed.
'''

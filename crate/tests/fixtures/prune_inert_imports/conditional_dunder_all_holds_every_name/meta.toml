[docs]
previewable = true
title       = "An `__all__` Below Module Scope Holds Every Name"

description = '''
The `__all__` naming `dumps` sits inside an `if TYPE_CHECKING:` block rather than at module scope, so the rule cannot settle which names the module exports. **Every import holds**, leaving `from json import dumps` in place even though nothing else reads it.
'''

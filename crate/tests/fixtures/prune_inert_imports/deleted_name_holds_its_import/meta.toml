[docs]
previewable = true
title       = "A Name a `del` Unbinds Holds Its Import"

description = '''
Nothing reads `os` after the import, yet the `del os` statement requires the binding to exist before it can unbind it. **The import holds**, because removing it would leave the `del` raising `NameError`.
'''

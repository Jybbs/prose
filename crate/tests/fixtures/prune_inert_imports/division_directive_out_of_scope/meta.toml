[docs]
previewable = false
title       = "Other Future Directives Like Division Stay Out of Scope"

description = '''
`from __future__ import division` can still change what `1 / 2` evaluates to on legacy interpreters, so the directive is **out of scope**. The rule acts only on the `annotations` import, leaving every other `__future__` directive untouched.
'''

[docs]
previewable = true
title       = "A Name Listed in `__all__` Holds Its Import"

description = '''
Nothing in the module body reads `loads`, but `__all__ = ["loads"]` names it as part of the module's public interface. **The import holds** and no diagnostic is reported, because pruning it would break the re-export.
'''

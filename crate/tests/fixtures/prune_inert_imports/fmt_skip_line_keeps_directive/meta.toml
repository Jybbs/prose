[docs]
previewable = false
title       = "A Trailing `# fmt: skip` Keeps the Directive"

description = '''
The file carries no annotation, so `from __future__ import annotations` would otherwise be removed, and a trailing `# fmt: skip` marks its line. **The import stays**, because the pipeline drops every edit whose range overlaps a skipped line.
'''

[docs]
previewable = false
title       = "A Trailing `# fmt: skip` Holds the Directive in Place"

description = '''
A trailing `# fmt: skip` on the directive line **drops the edit through the pipeline's central suppression filter**, leaving the import in place even though the file carries no annotations to justify it.
'''

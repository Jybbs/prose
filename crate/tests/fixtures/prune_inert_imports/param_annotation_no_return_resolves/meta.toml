[docs]
previewable = false
title       = "A Parameter Annotation Without a Return Still Resolves"

description = '''
`fetch` annotates its parameter as `target: Result` but declares no return annotation, and the scan for annotations still finds it. `Result` is defined at module scope before `fetch`, so evaluating the annotation eagerly is safe and **the `from __future__ import annotations` directive is removed**.
'''

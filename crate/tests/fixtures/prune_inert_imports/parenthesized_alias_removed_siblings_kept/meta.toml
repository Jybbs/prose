[docs]
previewable = false
title       = "Parenthesized Multi-Line Import Drops Only the Alias"

description = '''
The `from __future__ import` statement spreads its names across lines inside parentheses, one name per line. **Only the `annotations` entry is deleted**, taking its line with it, leaving `division` and the parenthesized shape in place.
'''

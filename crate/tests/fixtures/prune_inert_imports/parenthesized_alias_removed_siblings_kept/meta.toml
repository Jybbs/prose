[docs]
previewable = false
title       = "A Parenthesized Directive Loses Only the `annotations` Row"

description = '''
The `from __future__ import` statement lists `annotations` and `division` one per row inside parentheses, and the file carries no annotation. **Only the `annotations` row is removed**, leaving `division` and the parentheses as written.
'''

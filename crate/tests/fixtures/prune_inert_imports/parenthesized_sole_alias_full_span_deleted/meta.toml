[docs]
previewable = false
title       = "A Parenthesized Directive With One Name Is Removed Whole"

description = '''
`annotations` is the only name inside a parenthesized `from __future__ import` spanning three rows, and `x = 1` is the only other statement. **All three rows of the statement are removed**, so no empty pair of parentheses is left behind.
'''

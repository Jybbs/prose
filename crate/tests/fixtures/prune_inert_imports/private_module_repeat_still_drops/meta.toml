[docs]
previewable = true
title       = "A Repeat Drops Even Out of a Private Module"

description = '''
Every name here comes out of an underscore-named module, which holds an unread name as part of the importing module's surface, so **`unread` stays**. That marker reads an unread name rather than a repeated statement, so **the second `thing` still drops** as the rebind it is.
'''

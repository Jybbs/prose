[docs]
previewable = false
title       = "A Quoted Annotation Keeps the First Import Past a Repeat"

description = '''
`from typing import List` appears twice, and only the string annotation `"List[int]"` reads `List`. **The repeat is removed and the first import stays**, because the parse of the quoted annotation reaches a name the binding table does not.
'''

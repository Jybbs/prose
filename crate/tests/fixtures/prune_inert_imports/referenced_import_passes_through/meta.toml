[docs]
previewable = false
title       = "A Read Import Passes Through Unchanged"

description = '''
`import os` binds `os`, and `os.getcwd()` inside `main` reads it. **The file passes through unchanged and nothing is reported**, because the binding table records a read for `os`, so the rule has no unreferenced name to remove.
'''

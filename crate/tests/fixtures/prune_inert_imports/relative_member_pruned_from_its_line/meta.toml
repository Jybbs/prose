[docs]
previewable = false
title       = "An Unread Member Prunes Off a Relative Import"

description = '''
`from .helpers import first, second` is relative, and only `first` is read, through `value = first()`. **`second` prunes off the import line**, the same as an unread member on an absolute import would.
'''

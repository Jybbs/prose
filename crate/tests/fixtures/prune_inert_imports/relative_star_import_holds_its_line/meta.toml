[docs]
previewable = false
title       = "A Relative Star Import Keeps Its Line"

description = '''
`from . import *` binds a set of names the rule cannot enumerate, and `value = 1` is the only other statement. **The import stays and nothing is reported**, because the rule never removes a star import, relative or absolute.
'''

[docs]
previewable = false
title       = "A Relative Star Import Keeps Its Line"

description = '''
`from . import *` hides its names exactly as an absolute star import does, the leading dot changing nothing about that. **The import stays and nothing is reported**, since the rule treats a relative star the same as any other.
'''

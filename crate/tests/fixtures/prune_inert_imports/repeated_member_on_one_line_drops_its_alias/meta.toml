[docs]
previewable = false
title       = "A Member Repeated on One Line Loses Its Second Copy"

description = '''
`from x import a, a` names `a` twice on one line, and `y = a` reads it. **The second `a` is removed from the line and the first stays**, leaving `from x import a`, because the repeat is one member rather than the whole statement.
'''

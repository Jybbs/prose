[docs]
previewable = true
title       = "A Sole Unread Import Loses Its Whole Line"

description = '''
`import json` binds only `json`, and nothing in the module reads it. **The whole line is removed**, and the blank line below it stays as the file's first line.
'''

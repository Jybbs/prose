[docs]
previewable = true
title       = "A Star Import Keeps Its Line"

description = '''
`from os.path import *` binds a set of names the rule cannot enumerate, and `value = 1` is the only other statement. **The import stays and nothing is reported**, because the rule cannot count references to names it cannot enumerate, so it never removes or reports a star import.
'''

[docs]
previewable = true
title       = "A Trailing Alias Strips Its Preceding Comma"

description = '''
`annotations` is the second name in `from __future__ import division, annotations`, and nothing in the module needs deferred evaluation. **Removing `annotations` also strips the comma that preceded it**, leaving `division` on its own line with its original formatting intact.
'''

[docs]
previewable = true
title       = "Names on the Configured Allow List Stay Quiet"

description = '''
`reassigned-constants.allow` lists `LOG_LEVEL`, so its two module-level assignments pass through silently even though the name is reassigned. **`CACHE_DIR` is not listed, so both of its assignments are still flagged**, one diagnostic per occurrence.
'''

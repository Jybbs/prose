[docs]
previewable = false
title       = "An Augmented Assignment Marks a Constant as Reassigned"

description = '''
`COUNTER = 0` is followed by `COUNTER += 1`. **Only the `COUNTER = 0` line is reported**, because the augmented assignment counts as a second write to the name and marks it reassigned, while carrying no diagnostic of its own.
'''

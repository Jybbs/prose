[docs]
previewable = true
title       = "Already-Conforming Source Surfaces Nothing"

description = '''
`__version__` is assigned once, and `main`'s body defines no module-level constant of its own. **The file passes through unchanged with no diagnostic emitted**, pinning that `reassigned-constants` is lint-only and never edits source, whether or not it finds anything to flag.
'''

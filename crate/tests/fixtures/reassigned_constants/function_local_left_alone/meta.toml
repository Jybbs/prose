[docs]
previewable = false
title       = "A Function-Local Constant Is Not Reported"

description = '''
`PI = 3.14` is assigned inside the body of `compute`. **No diagnostic is reported**, because the rule reads module-level `SCREAMING_CASE` assignments only and stops at every `def` and `class` boundary.
'''

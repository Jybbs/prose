[docs]
previewable = false
title       = "A Function Rebind Leaves the Module Constant Silent"

description = '''
`TIMEOUT = 30` is assigned once at module level, and the `TIMEOUT = 60` inside `override` creates a function-local binding rather than rewriting the module name. The lint counts module-scope writes only, so the module constant goes unreported.
'''

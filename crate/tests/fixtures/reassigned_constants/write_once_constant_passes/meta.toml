[docs]
previewable = true
title       = "Write-Once Constants Pass Whatever Their Value"

description = '''
`PI` and `MAX_RETRIES` are each assigned exactly once, the write-once pattern a `SCREAMING_CASE` name signals. The rule counts writes rather than reading values, so **it stays silent** on both, one a float and the other an int.
'''

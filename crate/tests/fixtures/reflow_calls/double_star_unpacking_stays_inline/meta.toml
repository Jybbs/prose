[docs]
canonical   = false
previewable = false
title       = "Double-Star Unpacking Blocks the Explode"

description = '''
`merge(alpha=1, beta=2, gamma=3, **overrides)` passes three keywords and a `**` unpacking with no parameter name of its own. **The whole call stays inline**, because the `**` argument cannot be written as a keyword row, so the count trigger passes the call over.
'''

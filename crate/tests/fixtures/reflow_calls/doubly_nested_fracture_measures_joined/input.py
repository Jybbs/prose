def emit(alpha):
    raise TypeError(outer_helper(inner_helper(alpha_value_name,
                                              beta_value_name),
                                 gamma_value_name_long))

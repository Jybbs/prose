[docs]
canonical   = false
previewable = true
title       = "A Fractured Keyword List Rejoins Tight"

description = '''
`wrap(alpha, beta=2, **rest)` arrives fractured, with `**rest` pushed onto a second line.

The joined form fits, so the call rejoins onto one row, and the keyword **keeps the tight `beta=2` spelling** a condensed call takes rather than gaining spaces around its `=`.
'''

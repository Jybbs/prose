[docs]
previewable = false
title       = "A Grouped Value Reshapes Inside Its Pair"

description = '''
A bare generator expression takes a grouping pair as it binds to its resolved parameter name, and the nested call inside it explodes in the same pass, so the pair sits outside the reshape rather than holding it off.
'''

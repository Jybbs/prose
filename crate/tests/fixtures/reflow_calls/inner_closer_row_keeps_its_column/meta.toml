[docs]
previewable = true
title       = "An Inner Closer Row Keeps Its Column"

description = '''
The exploded `type` call closes on a row whose leading `)` is its own and whose final `)` closes the enclosing tuple, so the argument block's move reads that row as interior and leaves the closer at the call's column rather than seating it on the argument indent.
'''

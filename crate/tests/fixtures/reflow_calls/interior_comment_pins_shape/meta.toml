[docs]
canonical   = false
previewable = false
title       = "An Interior Comment Pins the Layout as Written"

description = '''
The exploded `build` call carries `# keep this note anchored` on its `alpha=1` row. **The call is left exactly as written**, because a comment inside an argument list pins the layout, since reshaping the arguments could separate the comment from the line it annotates.
'''

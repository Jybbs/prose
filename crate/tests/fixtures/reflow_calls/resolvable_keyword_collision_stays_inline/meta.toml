[docs]
previewable = false
title       = "A Call Binding One Name Twice Stays Inline"

description = '''
The fourth positional `c` binds to `f`'s `color` parameter, the same parameter the trailing `color=1` keyword already names. Writing the call in keyword form would render `color=` twice, so the rule leaves the call inline as written.
'''

[docs]
canonical   = false
previewable = true
title       = "A Single-Line Collection Value Stays Inline"

description = '''
The `build` call explodes to one keyword per line, and `opts` carries the single-line dict `{"k": "v"}`. A collection already on one line has no rows to re-indent, so the value moves with its keyword and stays inline.
'''

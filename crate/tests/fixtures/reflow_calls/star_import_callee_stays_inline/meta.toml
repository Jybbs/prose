[docs]
previewable = true
title       = "A Star Import Keeps a Resolvable Call Positional"

description = '''
`from _bisect import *` can rebind `bisect_left` at runtime, so the `def` visible above the import is not a signature the rule can safely name arguments against. The keyword rewrite is skipped, leaving the four positionals exactly as written.
'''

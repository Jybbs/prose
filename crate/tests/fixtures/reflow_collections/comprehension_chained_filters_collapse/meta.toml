[docs]
previewable = false
title       = "A Comprehension With Two `if` Clauses Rejoins"

description = '''
The `eligible` comprehension carries two `if` clauses on rows of their own. **It rejoins onto one line, with `if person.is_active` and `if person.has_quorum` written in source order after the `for` clause**, because the joined form fits the budget.
'''

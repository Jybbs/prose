[docs]
previewable = false
title       = "A Comment Inside a Comprehension Keeps Its Breaks"

description = '''
The `squares` comprehension would fit on one line, and a comment trails its `value * value` row. **The comprehension is left as written**, because the joined form has no row for the comment to trail, so rejoining would drop it.
'''

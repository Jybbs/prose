[docs]
previewable = false
title       = "A Comprehension With Two `for` Clauses Rejoins"

description = '''
The `flattened` comprehension carries two `for` clauses on rows of their own. **It rejoins onto one line as `[cell for row in grid for cell in row]`, with the clauses in source order**, because the joined form fits the budget.
'''

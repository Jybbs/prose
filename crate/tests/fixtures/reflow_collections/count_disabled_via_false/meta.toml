[docs]
previewable = false
title       = "With `max-dict-entries` Off Width Is the Only Dict Trigger"

description = '''
`max-dict-entries = false` is set, and `disabled` is a five-entry dict that fits its line. **The dict stays inline**, because turning the count trigger off leaves width as the only reason a dict expands, however many entries it carries.
'''

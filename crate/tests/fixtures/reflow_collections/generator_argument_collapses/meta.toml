[docs]
previewable = false
title       = "A Generator Argument Rejoins Onto Its Call's Line"

description = '''
`sum` takes a generator as its only argument, written across two rows. **The generator rejoins into `sum(value for value in amounts)`**, because the joined form fits, and the call's own parentheses stand in for the generator's, so no second pair is written.
'''

[docs]
canonical   = false
previewable = false
title       = "A Member Aligned Under an Interior Bracket Keeps Its Columns"

description = '''
A member whose continuation rows align under its own opening bracket would land against nothing once the row it sits on moves, so the expansion carries the row and leaves the member's own columns as the source wrote them.
'''

[docs]
canonical   = false
previewable = true
title       = "A Held Call Member Hangs From the Item Column"

description = '''
Expanding `nested` puts `alpha`, the `helper` call, and `zeta` each on their own row, and the already multi-line call is held rather than laid out again. Its block moves with the row it lands on, hanging from the item column with `beta` and `gamma` re-indented beneath `helper(` instead of staying at the columns the source wrote them at.
'''

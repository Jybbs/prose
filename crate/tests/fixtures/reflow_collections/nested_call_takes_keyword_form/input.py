def build(alpha_parameter, beta_parameter, gamma_parameter):
    return alpha_parameter


pairs = [build("a much longer string value here", "another much longer string value", "a third value"), None]

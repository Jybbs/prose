[docs]
previewable = false
title       = "Pack Varying Widths in the Order Written"

description = '''
The `__all__` list alternates wide strings with two-character ones, and with no sort reaching it the expansion packs each row at the widths it carries in that order, so three entries share a row wherever the row they make stays within budget.
'''

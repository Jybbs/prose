[docs]
previewable = false
title       = "Already-Split Statements Pass Through With No Edit"

description = '''
Both `from pkg.sub import` statements are already in repeated-prefix form, and each fits the `import-line-length` budget of `40`. **Both lines pass through unchanged**, because they are already the lines a gather and repack to that budget would write.
'''

[docs]
previewable = false
title       = "Two Identical From-Imports Collapse to One"

description = '''
`from pkg import alpha` appears twice, and the gathered roster carries each member once. The pair settles as **a single statement**, so the duplicate line disappears rather than surviving as a merged line naming `alpha` twice.
'''

[docs]
previewable = false
title       = "A Function-Body Import Splits at Its Own Indent"

description = '''
`import os, sys` sits inside the body of `load`. **It splits into `import os` and `import sys`, each opening at the body's own column**, because the split reads the indent of the line it rewrites rather than the module margin.
'''

[docs]
previewable = true
title       = "A Relative Import Repeats Its Leading Dots"

description = '''
`from ..pkg import alpha, beta, delta, gamma` overflows the `import-line-length` budget of 35, forcing the members across two statements. **Each split line repeats the two leading dots in its prefix**, so both open with the same `from ..pkg import`.
'''

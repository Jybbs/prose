[docs]
previewable = false
title       = "A Star Import Keeps Its Own Line Against the Merge"

description = '''
`from pkg import *` and `from pkg import alpha` name the same module on consecutive lines. **They stay as two statements**, because `from pkg import *, alpha` is not valid Python, so a `*` never shares a line with a named member.
'''

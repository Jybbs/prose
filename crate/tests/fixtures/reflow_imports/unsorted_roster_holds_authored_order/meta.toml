[docs]
previewable = false
title       = "An Over-Long Roster Holds Its Authored Order Under `alphabetize-siblings = false`"

description = '''
The roster sorts only where [[alphabetize-siblings]] runs, so a project that turned sorting off keeps the **authored member order** across the split.
'''

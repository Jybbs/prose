[docs]
previewable = true
title       = "A Pair Linking a Wider Chain Holds Its Shape"

description = '''
Each pair carries precedence, so neither sheds, and each wraps one operand of the surrounding `and` rather than the whole condition. Breaking either one would open rows inside a row that overflows either way, so both hold and the overflow falls to [[line-overflow]] to report.
'''

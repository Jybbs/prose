[docs]
previewable = true
title       = "A Generator's Pair Stays"

description = '''
`squares = (item for item in items)` wraps a bare generator expression. **The pair stays as written**, because Python requires those parentheses for the generator to parse at all.
'''

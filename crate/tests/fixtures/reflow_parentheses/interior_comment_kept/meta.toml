[docs]
previewable = true
title       = "A Pair Holding a Comment Stays"

description = '''
`total = (  # running sum` opens a parenthesized expression whose first line carries a comment. Folding the break would strand `# running sum` off the line it describes, so the parentheses and the multi-line form stay exactly as written.
'''

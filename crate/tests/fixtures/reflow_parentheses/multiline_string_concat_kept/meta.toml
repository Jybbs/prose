[docs]
previewable = false
title       = "A Multi-Line String Concatenation Stays Wrapped"

description = '''
`message = (` wraps two adjacent string literals, `"first part "` and `"second part"`, joined by implicit concatenation rather than an operator. **The pair and its rows stay as written**, because the rule joins an operator chain only and a concatenation is not one.
'''

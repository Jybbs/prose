[docs]
previewable = false
title       = "A Nested Pair Is Removed Inside a Join"

description = '''
`ready = (` opens a multi-line pair whose first row, `(server.up)`, carries a redundant pair of its own. **The inner pair is removed along with the outer one as the interior joins**, so the condition reads `ready = server.up and cache.warm`.
'''

[docs]
canonical   = false
previewable = true
title       = "A Re-Seat Composes With a Held Boolean Group"

description = '''
A continuation row aligned to the shed opener follows it leftward through an indent-only edit at the row start. The pair nested on that same row holds one `and` group inside the surrounding `or` chain, so it stays rather than shedding, and the break lands it whole on a row of its own.
'''

[docs]
previewable = true
title       = "A One-Element Tuple's Pair Survives"

description = '''
`single = (item,)` uses its parentheses to form the one-element tuple rather than to wrap an already-complete expression. They stay exactly as written.
'''

[docs]
canonical   = true
previewable = true
title       = "A Redundant Grouping Pair Is Dropped"

description = '''
The parentheses in `total = (base + tax)` and `label = (name)` change nothing, because each expression parses the same without them. Both pairs shed, leaving `total = base + tax` and `label = name`.
'''

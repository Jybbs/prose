[docs]
previewable = true
title       = "A Single-Line Return Annotation's Pair Drops"

description = '''
The parentheses around `int` in `def total() -> (int):` wrap a single name and change nothing about how the annotation parses. The pair sheds, leaving the header as `def total() -> int:`.
'''

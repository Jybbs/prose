[docs]
previewable = true
title       = "A Single-Line Return Annotation's Pair Is Removed"

description = '''
`def total() -> (int):` wraps the single name `int` in a pair that changes nothing about how the annotation parses. **The pair is removed**, leaving `def total() -> int:`.
'''

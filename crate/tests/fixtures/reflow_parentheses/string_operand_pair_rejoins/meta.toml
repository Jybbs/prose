[docs]
previewable = true
title       = "A Chain Carrying a String Operand Rejoins"

description = '''
The interior is an operator chain, and one of its operands happens to hold a string literal.

The fold reads **whether closing the soft wrap would respace a string's own interior**, not what kind of leaf the chain happens to carry. Nothing here spans rows inside a string, so this pair joins onto one row and sheds exactly as one comparing against a number would.
'''

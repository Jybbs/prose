def build(alpha_parameter, beta_parameter, gamma_parameter, delta_parameter):
    return alpha_parameter


def run(value, fallback=build("a long string value here", "another long string value", "third long string value", "fourth")):
    return value

[docs]
previewable = false
title       = "A Lone Parameter Expands Bare"

description = '''
`render`'s single parameter `palette_descriptor_inventories` pushes the inline signature past `code-line-length`, so **it expands onto one bare line of its own**. Because the only parameter is also the last, the expanded form carries no trailing comma.
'''

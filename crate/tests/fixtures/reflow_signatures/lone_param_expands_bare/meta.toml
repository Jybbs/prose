[docs]
previewable = false
title       = "A Single Parameter Expands Onto One Bare Line"

description = '''
`render`'s only parameter, `palette_descriptor_inventories`, pushes the inline signature past `code-line-length`. **The parameter expands onto one line of its own with no trailing comma**, because the only parameter is also the last, and the last parameter ends bare.
'''

[docs]
canonical   = false
previewable = true
title       = "A Multi-Line String Default Expands the Signature and Keeps Its Floor"

description = '''
A default whose break falls inside one string literal expands the signature the same way any other row-spanning parameter does, while the literal stays at its source column, since moving it would pad the string's own interior.
'''

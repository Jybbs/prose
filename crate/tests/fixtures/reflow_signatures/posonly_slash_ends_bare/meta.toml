[docs]
previewable = false
title       = "A Trailing Positional-Only `/` Ends the Expanded Form Bare"

description = '''
`render` takes five positional-only parameters, and its parameter list ends on a bare `/` separator. **The signature expands one per line and ends on the bare `/` line with no trailing comma**, so the output parses.
'''

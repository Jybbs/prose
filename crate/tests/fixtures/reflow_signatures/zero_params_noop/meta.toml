[docs]
previewable = false
title       = "A Zero-Parameter Signature Is Left as Written"

description = '''
`render` takes no parameters, and its `def` line runs well short of the budget. **The `def` is left exactly as written**, because neither the `max-params` count nor the `code-line-length` measure can trigger an expansion.
'''

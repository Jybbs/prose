[docs]
previewable = false
title       = "A Module Docstring Documents No Signature"

description = '''
The module's own docstring carries an `Args:` section naming `host (str)`, and the file defines no function and no class. **The entry is not reported**, because a module docstring documents no signature and no class body, so the entry resolves against nothing.
'''

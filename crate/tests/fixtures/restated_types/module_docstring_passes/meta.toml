[docs]
previewable = false
title       = "A Module Docstring Documents No Signature"

description = '''
The module's own docstring carries an `Args:` section naming `host (str)`, but the file defines no function and no class. **The entry resolves against nothing and stays silent**, since a module docstring documents no signature and no class body.
'''

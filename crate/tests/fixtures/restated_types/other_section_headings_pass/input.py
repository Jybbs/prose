def resolve(value: int, error: str):
    """
    Resolve a value.

    Returns:
        value (int): The resolved value, sharing the parameter's name.

    Raises:
        error (str): Raised when the same-named parameter is set.
    """

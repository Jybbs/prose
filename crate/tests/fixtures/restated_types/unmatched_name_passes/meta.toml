[docs]
previewable = false
title       = "An Entry Naming No Parameter and an Entry Carrying No Type Group"

description = '''
`host`'s `Args:` entry carries no parenthesized type to restate, and `retries (int)` names no parameter `dial` actually declares. **Neither entry is flagged**, one having nothing to report and the other resolving against no member of the signature.
'''

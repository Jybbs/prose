[docs]
previewable = true
title       = "A Trailing Comment Rides the Rejoined Line"

description = '''
`x = 1 + \` continues onto a line ending in `# note`, and the two fold into `x = 1 + 2  # note`. The comment's width still counts toward the budget that settles whether the split rejoins onto one line or takes parentheses instead.
'''

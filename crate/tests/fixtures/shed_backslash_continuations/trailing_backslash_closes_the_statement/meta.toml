[docs]
previewable = false
title       = "A Backslash Ahead of the Closing Newline Leaves No Space"

description = '''
`x = 1 \` continues onto the newline that closes the statement, so nothing follows the backslash to join it to. It drops without inserting a separator, leaving `x = 1` with no trailing whitespace.
'''

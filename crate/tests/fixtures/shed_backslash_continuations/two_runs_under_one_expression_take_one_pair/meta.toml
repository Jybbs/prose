[docs]
previewable = true
title       = "Two Runs Under One Expression Take One Pair"

description = '''
Two separate backslash runs sit on different rows of the same condition, and a single outermost expression spans both of them.

The wrap parenthesizes **that expression once** and drops every backslash inside it, rather than inserting a pair around each run and leaving the condition wrapped twice over.
'''

[docs]
previewable = false
title       = "A Pair Whose Breaks a Nested Bracket Holds Sheds in Place"

description = '''
The pair around the comparison holds none of the breaks inside it, so it sheds in place and the rows inside the `find` call stay where the layout rules read them.
'''

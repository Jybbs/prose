[docs]
previewable = false
title       = "A Redundant Pair Around a Call Argument Drops"

description = '''
`record((event))` wraps its single argument `event` in an extra set of parentheses inside the call's own. The inner set sheds, reflowing the call to `record(event)`.
'''

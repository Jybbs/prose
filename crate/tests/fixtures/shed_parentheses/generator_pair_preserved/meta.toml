[docs]
previewable = true
title       = "A Generator's Pair Survives"

description = '''
`squares = (item for item in items)` wraps a bare generator expression, and Python requires those parentheses for the generator to parse at all. They stay rather than shed.
'''

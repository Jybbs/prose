[docs]
previewable = false
title       = "A Multi-Line Return Annotation Is Left Alone"

description = '''
`def total() -> (` opens a multi-line return annotation spanning `int | str`. The parentheses stay, leaving `reflow-signatures` to reshape them across the expanded signature.
'''

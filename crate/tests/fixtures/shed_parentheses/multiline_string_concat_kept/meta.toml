[docs]
previewable = false
title       = "A Multi-Line String Concatenation Stays Wrapped"

description = '''
`message = (` wraps two adjacent string literals, `"first part "` and `"second part"`, joined by implicit concatenation rather than an operator. Because that interior is not an operator tree, the parentheses stay instead of folding onto one line.
'''

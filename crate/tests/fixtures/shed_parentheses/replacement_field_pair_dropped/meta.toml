[docs]
previewable = false
title       = "A Redundant Pair Inside a Replacement Field Sheds"

description = '''
A grouping pair that binds nothing sheds wherever it sits, an f-string replacement field included, so the interpolated expression reads the same as one written outside a string.
'''

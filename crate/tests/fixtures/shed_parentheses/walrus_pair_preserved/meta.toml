[docs]
previewable = true
title       = "A Walrus Binding's Pair Survives"

description = '''
The line `result = (count := total)` passes through unchanged, because a pair around a `:=` binding is never shed. Keeping the parentheses in every context lets the walrus read as one consistent shape rather than a positional exception.
'''

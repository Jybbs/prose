[docs]
previewable = true
title       = "A Comment Inside the Header Holds It"

description = '''
Removing `object` from `Annotated`'s base list would also remove the comment sharing its line, and dropping `Bare`'s empty parentheses would take `# no bases at all` with them. A deletion span that holds a comment is skipped, so both headers stay exactly as written.
'''

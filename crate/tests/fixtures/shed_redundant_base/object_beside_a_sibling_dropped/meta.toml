[docs]
previewable = true
title       = "`object` Beside a Real Base Goes Alone"

description = '''
`class Layered(Mapping, Sized, object):` and `class Trailing(Mapping, object):` each name a real base alongside `object`. Only the `object` entry and the comma binding it to the preceding base drop, leaving `class Layered(Mapping, Sized):` and `class Trailing(Mapping):` with their parentheses kept.
'''

[docs]
previewable = true
title       = "A Rebound `object` Names a Real Base"

description = '''
`object = LegacyBase` rebinds the name `object` at module level before `class Consumer(object):` is reached. The base in `Consumer`'s header names that local binding rather than the `object` builtin, so the header stays exactly as written.
'''

[docs]
previewable = false
title       = "A Call Already in the Bare Form Passes Through Untouched"

description = '''
`Button.render` already calls `super().render()` in the bare form. **The module passes through untouched**, because the rule reads only the two-argument `super(Class, self)` form.
'''

[docs]
previewable = false
title       = "A Call Already in the Bare Form Draws No Edit"

description = '''
`Button.render` already calls `super().render()` in the bare form. Only the two-argument `super(Class, self)` form is read, so the module passes through untouched.
'''

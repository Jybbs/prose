[docs]
previewable = false
title       = "A Call Outside Any Class Stays Written"

description = '''
A module-level function calling `super(Child, instance)` keeps its arguments, because no enclosing class stands where the bare form would read one.
'''

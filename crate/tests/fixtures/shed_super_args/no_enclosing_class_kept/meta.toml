[docs]
previewable = false
title       = "A Call Outside Any Class Stays Written"

description = '''
`bind`, a module-level function, calls `super(Child, instance).probe` outside any class body. Its arguments stay written, because no enclosing class stands where the bare form would read one.
'''

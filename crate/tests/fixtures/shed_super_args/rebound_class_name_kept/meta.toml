[docs]
previewable = false
title       = "A Local Name Shadowing the Class Keeps the Call Written"

description = '''
`Child.total(self, Child)` names its own second parameter `Child`, shadowing the class name in the method's own scope. `super(Child, self)` stays written, because that first argument reads the parameter's binding rather than the enclosing class.
'''

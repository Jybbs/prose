[docs]
previewable = true
title       = "Already-Annotated Members Stay Silent"

description = '''
`configured` takes an annotated `name: str` alongside the unannotated `count`, and its return type is annotated `-> int`. `signature-annotations` reports only `count`, leaving the annotated parameter and the annotated return unflagged.
'''

[docs]
canonical   = true
previewable = true
title       = "A Literal Default Suggests the Parameter's Type"

description = '''
`scale`'s only parameter, `factor`, defaults to the float literal `2.0` and carries no annotation. `signature-annotations` reports the gap and attaches a display-only suggestion, `factor: float`, that the formatter records but never auto-applies.
'''

[docs]
previewable = false
title       = "A Multi-Line Default Keeps the Signature One Per Line"

description = '''
A signature the author already laid out one parameter per line around a multi-line default re-emits at that shape, its default's block hanging from the parameter column. The trailing comma goes to `strip-trailing-commas` rather than surviving the reshape.
'''

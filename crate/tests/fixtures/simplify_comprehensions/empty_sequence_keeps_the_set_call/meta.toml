[docs]
previewable = true
title       = "An Empty Sequence Leaves `set()` Standing"

description = '''
`set([])` and `set(())` each carry an empty sequence literal as their argument. Because `{}` already names an empty dict, `simplify-comprehensions` collapses both to the bare `set()` call rather than a brace form.
'''

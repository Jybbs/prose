[docs]
previewable = true
title       = "A Filtered or Chained Comprehension Stands"

description = '''
`active`'s comprehension carries an `if row.live` guard, and `nested`'s carries two `for` clauses chaining over two levels. Neither reads as a plain copy of a single iterable, so `simplify-comprehensions` leaves both comprehensions exactly as written.
'''

[docs]
previewable = true
title       = "A Filtered or Chained Comprehension Stands"

description = '''
Nothing here changes. `active`'s comprehension carries an `if row.live` guard, and `nested`'s carries two `for` clauses chaining over two levels.

Neither reads as **a plain copy of a single iterable**, which is the only shape this rule collapses, so both comprehensions are left exactly as written.
'''

[docs]
canonical   = true
previewable = true
title       = "A List or Tuple Literal Inside `set()` Becomes a Set Literal"

description = '''
A bracketed literal handed to `set()` already spells its members, so the call gives way to the brace form and `set([1, 4, 9])` reads as `{1, 4, 9}`.
'''

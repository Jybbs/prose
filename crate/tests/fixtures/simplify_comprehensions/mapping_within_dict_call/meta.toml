[docs]
previewable = true
title       = "A Dict Literal Inside `dict()` Stands on Its Own"

description = '''
`dict({"size": 10})` wraps a dict literal that is already the mapping the call would build. **The call is removed**, so `config` becomes `{"size": 10}`.
'''

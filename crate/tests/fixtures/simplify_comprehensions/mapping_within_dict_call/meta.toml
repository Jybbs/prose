[docs]
previewable = true
title       = "A Mapping Inside `dict()` Stands On Its Own"

description = '''
`dict({"size": 10})` wraps a dict literal that is already the mapping it would build, so the call is dropped and `config` becomes `{"size": 10}`.
'''

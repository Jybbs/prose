[docs]
previewable = false
title       = "A Comprehension Whose Target Does Not Match Its Entry Stands"

description = '''
The comprehension's target `pair` binds one name while its entry `key: value` names two others, so the comprehension is not copying `rows` and no `dict(...)` call could spell what it builds. `paired` stays exactly as written.
'''

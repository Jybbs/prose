[docs]
previewable = false
title       = "A Clean File Passes Through Unchanged"

description = '''
`main` binds no name at all, so the file holds nothing for this lint to flag. The run produces **no diagnostic and no edit**, pinning the rule's lint-only shape wherein a clean input passes through exactly as written.
'''

[docs]
previewable = false
title       = "Bindings Inside Class Methods Are Flagged"

description = '''
`normalized` inside `Service.consume` **is flagged** the same as any single-use binding in a plain function, because `single-use-variables` walks into class bodies and evaluates each method's scope on its own. Methods carry no blanket exemption from the rule.
'''

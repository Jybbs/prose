[docs]
previewable = false
title       = "Already-Canonical Spacing Passes Through Unchanged"

description = '''
Every gap in this file already matches the canonical count. Two blank lines separate the module-level definitions, one blank line separates `first` from `second`, `Posting`'s docstring sits flush against its class header, one blank line separates `Sized`'s header from `capacity`, and one blank line separates `beta`'s header from the `match` opening its body. **The rule emits no edit**, so the file passes through unchanged.
'''

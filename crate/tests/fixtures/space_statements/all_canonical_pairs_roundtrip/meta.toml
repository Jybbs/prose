[docs]
previewable = false
title       = "Already-Canonical Spacing Round-Trips Unchanged"

description = '''
Every gap in this file already matches the canonical spacing, so it **round-trips with no edit at all**.

Two blank lines separate the module-level definitions, one blank sits between `first` and `second`, `Posting`'s docstring touches its class header directly, one blank separates `Sized`'s header from `capacity`, and one blank precedes the `match` opening `beta`'s body.
'''

[docs]
previewable = false
title       = "An Assignment Run Keeps the Gaps It Was Written With"

description = '''
`PORT`, `HOST`, and `TIMEOUT` are module-level assignments written with no blank line between them. **Each gap stays exactly as written**, because the rule sets no canonical count for one assignment after another, so the source passes through unchanged.
'''

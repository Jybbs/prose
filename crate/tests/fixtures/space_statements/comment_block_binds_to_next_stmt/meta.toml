[docs]
previewable = false
title       = "An Own-Line Comment Binds to the Statement Below It"

description = '''
The comment `# describes the second helper` sits between `first` and `second` with no blank line on either side. **The comment becomes `second`'s leading block**, so the canonical two blank lines are written above the comment and the comment stays flush against `def second():`.
'''

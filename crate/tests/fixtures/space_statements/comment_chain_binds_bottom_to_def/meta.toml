[docs]
previewable = false
title       = "A Run of Comments Binds as One Block to the Def"

description = '''
Three consecutive comments sit between `first` and `second` with no blank line on either side. **The three comments form one leading block for `second`**, so two blank lines are written above the topmost `# explains why`, the block keeps its lines together, and `# second helper` stays flush against `def second():`.
'''

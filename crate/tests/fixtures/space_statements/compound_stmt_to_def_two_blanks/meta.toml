[docs]
canonical   = true
previewable = true
title       = "Compound Statement Before a Def Takes Two Blanks"

description = '''
The `for setup in initial_steps:` loop sits one blank line above `def main():`. **Two blank lines precede every module-level `def`**, so the gap widens to two, giving the reader's eye an anchor between top-level units.
'''

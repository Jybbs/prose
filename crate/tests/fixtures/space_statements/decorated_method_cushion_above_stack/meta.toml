[docs]
previewable = false
title       = "Field-to-Decorated-Method Cushion Sits Above the Stack"

description = '''
A class field directly followed by a decorated method carries one blank line of cushion. The cushion sits **above the topmost decorator** rather than between the decorator and the def, so the decorator stack reads as bound to the method below.
'''

[docs]
previewable = false
title       = "A Flanked-Label Banner Divides Like a Full Rule"

description = '''
`# --- Auxiliary Helpers ---`, a label flanked by short dash runs, reads as a **section divider rather than a comment describing `second`**. The canonical two-blank gap after `first` lands above the banner, and one blank line separates the banner from the `def second():` below.
'''

[docs]
previewable = false
title       = "A Flanked-Label Banner Divides Like a Full Rule"

description = '''
`# --- Auxiliary Helpers ---` is a label flanked by short dash runs, which reads as a **section divider rather than a comment describing `second`**.

The canonical two-blank gap after `first` therefore lands above the banner, and one blank line separates the banner from the `def second():` below it.
'''

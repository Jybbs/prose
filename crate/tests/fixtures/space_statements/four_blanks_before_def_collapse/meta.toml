[docs]
previewable = false
title       = "Four Blank Lines Before a Def Collapse to Two"

description = '''
Four blank lines separate `first` from `def second():`, twice the canonical count before a module-level `def`. **The four collapse to two**, because the rule replaces the whole whitespace run between the two statements in one edit rather than removing one line at a time.
'''

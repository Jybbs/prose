[docs]
previewable = false
title       = "An Indented Hash Heading Divides Methods"

description = '''
The indented `### Lifecycle` heading sits between the methods `open` and `close` inside `Session`, flush against both. **The heading reads as a section divider at class scope too**, so one blank line is written above it and one below, separating the two methods around it rather than binding the heading to `close` as its leading comment.
'''

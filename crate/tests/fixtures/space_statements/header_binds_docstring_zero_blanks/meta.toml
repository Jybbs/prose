[docs]
previewable = false
title       = "A Class Header Binds Its Docstring With No Blank Line"

description = '''
`Bound`'s docstring sits flush against the class header, and one blank line separates it from `def follow_up(self):`. **Both gaps already match the canonical counts, no blank line between a header and its docstring and one between the docstring and the next member**, so the source passes through unchanged.
'''

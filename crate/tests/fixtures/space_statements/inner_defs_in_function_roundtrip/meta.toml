[docs]
previewable = false
title       = "Nested Function Defs Round-Trip Unchanged"

description = '''
`inner_a` and `inner_b` sit flush against each other inside `outer`'s body, and **no blank lines are inserted between them**. Function scope carries no canonical blank-line counts, so the nested defs round-trip unchanged whatever spacing the source wrote.
'''

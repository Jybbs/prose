[docs]
previewable = false
title       = "Nested Defs Inside a Function Pass Through Unchanged"

description = '''
`inner_a` and `inner_b` sit flush against each other inside `outer`'s body. **No blank line is inserted between them**, because function scope carries no canonical count between definitions, so the nested defs pass through with whatever spacing the source wrote.
'''

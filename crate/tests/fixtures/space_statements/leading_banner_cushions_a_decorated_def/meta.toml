[docs]
previewable = false
title       = "A Leading Banner's Blank Line Sits Above the Decorator"

description = '''
The `# Section: Handlers` banner opens the file directly above `@decorator` and `def handle():`. **The divider's blank line is written above `@decorator` rather than between it and the `def`**, because a decorated definition starts at its first decorator.
'''

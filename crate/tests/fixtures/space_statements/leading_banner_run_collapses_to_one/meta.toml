[docs]
previewable = false
title       = "The Blank Run Below a Leading Banner Collapses to One Line"

description = '''
Three blank lines separate the `# Section: Configuration` banner opening the file from `import os`. **The run below a leading block caps at one blank line**, so the three collapse to the single blank line a divider keeps.
'''

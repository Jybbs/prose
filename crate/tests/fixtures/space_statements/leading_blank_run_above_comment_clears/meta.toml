[docs]
previewable = false
title       = "A Blank Run Above a Module's Leading Comment Clears"

description = '''
The run opening a module is **measured above the comment block leading the first statement** rather than above the statement itself, so a module whose first line is an own-line comment clears the same run. The comment's own tight binding to the statement below is unchanged.
'''

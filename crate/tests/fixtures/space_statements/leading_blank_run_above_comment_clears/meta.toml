[docs]
previewable = false
title       = "A Blank Run Above a Leading Comment Clears"

description = '''
The file opens with two blank lines above `# Configure the sink.`, which sits flush against `import os`. **Both blank lines clear**, because the module's leading run is measured above the comment block that leads the import rather than above the import itself, and the comment stays flush against the import.
'''

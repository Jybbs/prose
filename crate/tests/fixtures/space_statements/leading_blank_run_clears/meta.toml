[docs]
previewable = false
title       = "A Blank Run Opening a Module Clears"

description = '''
Two blank lines sit above `import os` at the top of the file, and they **clear entirely**, so the module starts on its first statement. The single blank between the import and `x = 1` stays as the source wrote it.
'''

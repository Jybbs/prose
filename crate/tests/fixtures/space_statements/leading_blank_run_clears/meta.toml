[docs]
previewable = false
title       = "A Blank Run Opening a Module Clears"

description = '''
The file opens with two blank lines above `import os`. **Both blank lines clear**, so the module starts on its first statement, and the one blank line between the import and `x = 1` stays as written.
'''

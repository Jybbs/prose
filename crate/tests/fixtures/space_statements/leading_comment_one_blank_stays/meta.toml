[docs]
previewable = false
title       = "One Blank Line Below a Leading Description Stays"

description = '''
`# Configure the sink.` opens the file and describes `import os` rather than dividing a section, with one blank line between the two. **The blank line the author wrote stays**, whereas the same block between two statements would bind flush against the import.
'''

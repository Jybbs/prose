[docs]
previewable = false
title       = "A Module's Leading Pragma Cushions the First Statement"

description = '''
The `# ruff: noqa: F401` pragma opens the file tight against `import os`. **A tool pragma anchors in place the way a suppression directive does**, so the divider blank seats below it rather than binding it to the import.
'''

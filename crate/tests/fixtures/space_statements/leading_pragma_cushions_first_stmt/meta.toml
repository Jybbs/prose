[docs]
previewable = false
title       = "A Leading Pragma Takes One Blank Line Before the First Statement"

description = '''
The `# ruff: noqa: F401` pragma opens the file flush against `import os`. **One blank line is written below the pragma**, because a tool pragma stays in place the way a suppression directive does rather than binding to the import as a leading comment.
'''

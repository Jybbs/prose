[docs]
previewable = false
title       = "Methods in a Doubly Nested Class Take One Blank Line"

description = '''
`alpha` and `beta` sit three blank lines apart inside `Inner`, itself nested inside `Outer`. **The three blank lines collapse to one**, because the class-scope count applies at any nesting depth, and both class headers gain one blank line before their first member.
'''

[docs]
previewable = false
title       = "Methods in a Doubly Nested Class Carry One Blank"

description = '''
`alpha` and `beta` live in `Inner`, itself nested inside `Outer`, and the three blank lines between them **collapse to one**, because the class-scope canonical applies at any nesting depth. Both class headers also gain their one blank before the first member below them.
'''

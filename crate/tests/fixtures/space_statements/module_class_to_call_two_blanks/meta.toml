[docs]
previewable = false
title       = "A Call After a Module-Level Class Takes Two Blank Lines"

description = '''
The bare call `register(Registry)` sits flush below the end of `class Registry:`. **The gap widens to two blank lines**, the canonical count after a module-level class whatever the source wrote, and the class header gains its one blank line before the `entries` field.
'''

[docs]
previewable = false
title       = "A Def After a Module-Level Class Takes Two Blank Lines"

description = '''
`def helper():` opens on the line directly after `Posting`'s body ends. **The gap widens to two blank lines**, the canonical count between a module-level class and the `def` after it, whatever count the source had, and the class header gains its one blank line before `title`.
'''

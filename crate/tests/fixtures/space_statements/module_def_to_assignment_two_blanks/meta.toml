[docs]
previewable = false
title       = "An Annotated Assignment After a Def Takes Two Blank Lines"

description = '''
`TIMEOUT: float = 30.0` sits on the line directly after `configure`'s body ends. **The gap widens to two blank lines**, the canonical count after a module-level `def`, even though the source had none.
'''

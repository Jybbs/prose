[docs]
previewable = false
title       = "An Annotated Assignment after a Def Carries Two Blanks"

description = '''
`TIMEOUT: float = 30.0` follows directly on the line after `configure`'s body ends. **The gap after a module-level `def` normalizes to two blank lines**, so the annotated assignment gains that spacing even though the source held none.
'''

[docs]
previewable = false
title       = "A Control-Flow Block After a Def Takes Two Blank Lines"

description = '''
The `for item in queue:` loop opens on the line directly after `load`'s body ends. **The gap widens to two blank lines**, the canonical count after a module-level `def`, so the loop moves down even though the source had no gap at all.
'''

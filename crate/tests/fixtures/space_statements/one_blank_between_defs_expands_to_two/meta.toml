[docs]
previewable = false
title       = "One Blank Line Between Module Defs Widens to Two"

description = '''
`first` and `second` are separated by one blank line, one short of the count between module-level definitions. **The gap widens to two blank lines**, because the rule rewrites the whitespace between the two statements rather than moving either `def`.
'''

[docs]
previewable = false
title       = "A Semicolon-Joined Pair Takes No Gap"

description = '''
`from collections import abc` and `import sys` share one physical line, joined by `;`. **The pair gets no edit**, because `import sys` opens no line of its own, so there is no blank run above it to set, and the line passes through unchanged.
'''

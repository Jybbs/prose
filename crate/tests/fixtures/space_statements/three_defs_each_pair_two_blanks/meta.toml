[docs]
previewable = false
title       = "Three Back-to-Back Defs Each Take Two Blank Lines"

description = '''
`beta` sits flush below `alpha`, and one blank line separates `beta` from `gamma`. **Each adjacent pair is measured on its own and widens to two blank lines**, so the zero-blank gap and the one-blank gap end up at the same count.
'''

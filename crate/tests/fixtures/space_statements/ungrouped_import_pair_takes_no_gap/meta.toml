[docs]
previewable = false
title       = "An Import Pair Takes No Gap without the Grouping Rule"

description = '''
`from sys import argv` and `import os` sit on adjacent lines with [[group-imports]] not running. **The pair keeps the shape its author wrote**, because `space-statements` alone treats two imports as ordinary neighboring statements, leaving no boundary between them for a blank line to mark.
'''

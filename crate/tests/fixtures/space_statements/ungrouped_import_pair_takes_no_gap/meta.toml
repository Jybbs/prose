[docs]
previewable = false
title       = "An Import Pair Takes No Gap Without the Grouping Rule"

description = '''
`from sys import argv` and `import os` sit on adjacent lines with [[group-imports]] not running. **The pair stays as written**, because `space-statements` alone reads two imports as ordinary neighboring statements and sets no count between them, so no blank line marks a boundary.
'''

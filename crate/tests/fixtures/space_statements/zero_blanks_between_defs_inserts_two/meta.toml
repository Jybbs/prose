[docs]
previewable = false
title       = "No Blank Line Between Module Defs Widens to Two"

description = '''
`def second():` opens on the line directly after `first`'s `return 1`, with no blank line between the two defs. **Two blank lines are inserted**, the canonical count between module-level definitions, because the rule writes the missing newlines into the whitespace between the two statements.
'''

[docs]
previewable = true
title       = "A Run Inside the Budget Keeps Its Line"

description = '''
`message`'s run joins two literals onto one line that clears `code-line-length`, `"a short clause " "and another"`. `stack-adjacent-strings` **leaves it on one line**, because the break answers to the budget rather than to the mere presence of a concatenation.
'''

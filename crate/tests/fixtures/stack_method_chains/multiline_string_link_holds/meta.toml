[docs]
canonical   = false
previewable = false
title       = "A Link Spanning a Multi-Line String Keeps the Chain as Written"

description = '''
The `.first(` link carries a triple-quoted string whose line break is string content, so no settling brings that link onto one row. **The chain stays as written**, because a link still spanning lines when the chain is read blocks the restack.
'''

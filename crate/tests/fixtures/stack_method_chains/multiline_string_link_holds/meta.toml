[docs]
canonical   = false
previewable = false
title       = "A Link Spanning a Multi-Line String Holds the Chain"

description = '''
The `.first(` link carries a triple-quoted string whose line break is string content, so no settling can bring that link onto one row. The link still spans lines when the chain is read, and a chain holding such a link stays where the source put it.
'''

[docs]
previewable = false
title       = "A Chain Inside a Broken Chain's Link Breaks Where It Lands"

description = '''
Breaking the outer chain moves the `relative_to` link to the hang column, and the chain inside its argument trips from there, so it breaks in the same text.
'''

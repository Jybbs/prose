[docs]
previewable = false
title       = "A Nested Chain That Fits Where It Ends Up Stays Joined"

description = '''
Breaking the outer chain hangs the `.relative_to` link, and the `root.resolve().parent()` chain inside its argument fits the `60`-column budget from that column. **The inner chain stays joined on the link's row.**
'''

[docs]
canonical   = false
previewable = false
title       = "A Zero `max-shift` Takes the Full Split Every Time"

description = '''
`max-shift = 0` forbids any hang, so **even the short receiver `query` stands alone on its row** rather than keeping `.filter(a)` beside it. Every link is written flush beneath the receiver at one indent, the full split applying to every chain the rule breaks.
'''

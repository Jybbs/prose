[docs]
canonical   = false
previewable = true
title       = "A Two-Link Chain Stays on One Line"

description = '''
`obj.first(alpha).second(beta)` holds two links, at or under `max-links`, and its single-line form fits the width budget, so the line is left exactly as written. The rule breaks chains long enough to read as pipelines rather than every attribute call.
'''

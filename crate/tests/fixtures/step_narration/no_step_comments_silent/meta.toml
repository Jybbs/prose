[docs]
previewable = true
title       = "Source Without Numbered Comments Surfaces Nothing"

description = '''
`process` carries no comments at all, so there is no numbered-step narration for the lint to flag and the run produces **no diagnostics**. The rule is lint-only, leaving the source byte-for-byte unchanged.
'''

[docs]
previewable = true
title       = "Numeric-Dot Shape (`# 1.`) Is Matched"

description = '''
The comment `# 1. normalize whitespace` inside `process` matches the **numeric-dot shape**, a digit followed by `.` and whitespace, so the lint reports it and recommends extracting the step as a named function. The source itself is left unchanged, because the rule is lint-only.
'''

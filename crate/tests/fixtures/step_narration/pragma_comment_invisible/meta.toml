[docs]
previewable = true
title       = "Pragma-Style Comments Stay Quiet"

description = '''
`# noqa: F401`, `# type: ignore`, and `# pyright: ignore[reportMissingImports]` are tool directives rather than step narration, and **the lint skips pragma comments entirely**. The run reports nothing, leaving the module byte-for-byte unchanged.
'''

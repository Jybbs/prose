[docs]
previewable = false
title       = "Step Word With a Colon Separator Is Flagged"

description = '''
The comment `# Step 1: validate input` inside `process` matches the **step-word shape with a `:` separator**, wherein the literal word `Step` plus a number is enough and `:` stands in for the dot. The lint reports one diagnostic and leaves the source unchanged.
'''

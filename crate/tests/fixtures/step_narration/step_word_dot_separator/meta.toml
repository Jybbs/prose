[docs]
previewable = false
title       = "Step Word With a Dot Separator Is Flagged"

description = '''
The comment `# Step 1. validate input` inside `process` matches the **step-word shape with a `.` separator**, because the word `Step` and a number trigger the lint whether the separator is `.` or `:`. One diagnostic is reported and the source is left unchanged.
'''

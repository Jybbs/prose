[docs]
previewable = false
title       = "A `Step 1.` Comment Is Reported"

description = '''
`# Step 1. validate input` sits on its own line inside `process`. **`step-narration` reports it**, because the word `Step` followed by a number matches with a `.` separator as well as a `:`, and the source is left as written.
'''

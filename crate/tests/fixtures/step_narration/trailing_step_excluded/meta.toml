[docs]
previewable = true
title       = "Inline End-of-Line Comments Stay Quiet"

description = '''
Nothing is reported here. The `# 1. trim whitespace` comment sits at the end of the `cleaned = payload.strip()` line rather than on a line of its own.

**Only comments occupying their own line are checked**, so this one is skipped even though its text matches the numbered-step shape exactly.
'''

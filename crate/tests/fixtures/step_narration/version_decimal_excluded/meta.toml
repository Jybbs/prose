[docs]
previewable = false
title       = "A Decimal Version Comment Is Skipped"

description = '''
`# 1.2 release notes` and `# 2.0 spec compliance` each put a digit directly after the `.`. **`step-narration` reports neither**, because the numbered-step form requires whitespace after the `.`, and a decimal version has a digit there instead.
'''

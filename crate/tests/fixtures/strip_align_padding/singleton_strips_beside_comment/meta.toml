[docs]
previewable = false
title       = "Singleton Strips Despite Leading or Trailing Comments"

description = '''
`stays_singleton` carries `# leading comment lives on its own line` above its one entry, and `also_stripped` carries `# trailing comment after the value` beside its entry on the same line, yet `strip-align-padding` **strips the pre-`:` padding on both**. The edit removes only the whitespace between the key and its `:`, structurally separate from either comment, so neither comment is touched.
'''

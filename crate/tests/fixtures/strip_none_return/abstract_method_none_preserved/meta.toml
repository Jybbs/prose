[docs]
previewable = false
title       = "An Abstract Method Stub's None Return Annotation Survives"

description = '''
`Base.run` is decorated `@abstractmethod` and its body is a single `...`, so `strip-none-return` leaves its `-> None` untouched. The placeholder stands in for an implementation, not a demonstrated return of nothing, which is the shape the rule strips.
'''

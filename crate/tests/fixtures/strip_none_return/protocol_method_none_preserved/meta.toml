[docs]
previewable = false
title       = "A Protocol Method Stub's None Return Annotation Survives"

description = '''
`Reader.close` is a `Protocol` method whose entire body is a single `...`, and `strip-none-return` leaves its `-> None` untouched. The placeholder carries the contract a structural type checker reads, not a redundant annotation the rule strips.
'''

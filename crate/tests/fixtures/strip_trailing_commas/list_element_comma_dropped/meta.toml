[docs]
previewable = true
title       = "Lists Follow the Same Strip as Dicts"

description = '''
`retired`'s last element, `"INST_2021_0044",`, carries a `,` before the list's closing `]`. **`strip-trailing-commas` scans backward from that `]` and lands on the comma**, applying the identical path used to strip dict values.
'''

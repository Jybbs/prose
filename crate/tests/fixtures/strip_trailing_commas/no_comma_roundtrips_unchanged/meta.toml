[docs]
previewable = true
title       = "Already-Stripped Source Is Left Alone"

description = '''
`Posting(company="Cianbro", title="Electrician")` already ends without a trailing `,` before its closing `)`. **`strip-trailing-commas` scans backward, reaches `"Electrician"` rather than a comma, and emits no edit**, so the source round-trips exactly as written.
'''

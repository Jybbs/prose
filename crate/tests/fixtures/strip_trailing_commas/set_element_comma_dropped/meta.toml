[docs]
previewable = false
title       = "A Set Literal Drops the Trailing Comma"

description = '''
`allowed` spreads three strings over one line each, and the last, `"INST_2021_0044",`, carries a `,` before the closing `}`. **That comma is removed**, by the same read from the closing bracket that strips a dict.
'''

[docs]
previewable = false
title       = "Set Literals Drop the Trailing Comma"

description = '''
`allowed`'s last element, `"INST_2021_0044",`, carries a `,` before the set's closing `}`. **`strip-trailing-commas` scans backward from that `}` and lands on the comma**, matching the path used for dicts.
'''

[docs]
previewable = false
title       = "Skip on a Wrapped Condition Pins the Header, Not the Body"

description = '''
A `# fmt: skip` trailing a wrapped `if` header **covers the header's own logical line only**, so the header keeps its parentheses while `strip-trailing-commas` still reaches the indented body below it.
'''

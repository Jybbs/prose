[docs]
previewable = false
title       = "A Leading Header Settles Above a Sorted Import Block"

description = '''
A module opening on a license header leads an import block the pipeline reorders beneath it. **The header holds the single blank the author left below it while `group-imports`, `alphabetize`, and `align-imports` reshape the run**, so the head clause settles against every rule acting on the statements it heads.
'''

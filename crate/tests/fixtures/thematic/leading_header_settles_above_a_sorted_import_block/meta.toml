[docs]
previewable = false
title       = "A Leading Header Stays Above a Sorted Import Block"

description = '''
A two-line copyright header opens the file above an import run written out of order. **The header keeps the single blank line its author left below it** while the imports sort to `numpy`, `os`, `requests` and the `from collections` import stays in its own group beneath them, so the header settles the same way against every rule acting on the statements it heads.
'''

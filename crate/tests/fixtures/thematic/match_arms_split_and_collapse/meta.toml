[docs]
previewable = false
title       = "Match Arms Split or Collapse by Budget, Kwargs Sort"

description = '''
`align_match_case` splits the three over-budget arms onto their own lines and collapses the under-budget `case _` fallback inline. **Kwargs inside the dispatched calls sort, dangling commas drop, and the lone-key `SETTINGS` dict holds the break its author wrote**, with a second pass stable.
'''

[docs]
previewable = false
title       = "A Trailing Comment Widens the Import Row It Joins"

description = '''
The comment sits directly beneath the import with no blank line between them, so it reads as that statement's trailing comment and **joins the import's row**. The forecast measures the row at its joined width rather than at the import's own, so the run is seated against the width it actually lands at.
'''

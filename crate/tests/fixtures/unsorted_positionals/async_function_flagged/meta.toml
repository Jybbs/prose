[docs]
previewable = false
title       = "An `async` Function Is Reported Like a Plain One"

description = '''
`fetch` is an `async def` whose parameters `target, source` are listed out of alphabetical order. **The lint reports the run exactly as it would for a plain `def`**, because a caller binds a coroutine's positional arguments by slot the same way.
'''

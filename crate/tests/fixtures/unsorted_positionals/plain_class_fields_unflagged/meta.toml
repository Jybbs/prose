[docs]
previewable = true
title       = "A Plain Class Draws No Report"

description = '''
`Endpoint`'s header names no constructor generator, so it binds no field by position and its out-of-order `timeout`, `method` run **gets no diagnostic**. [[alphabetize]] sorts these fields by name as an auto-fix instead.
'''

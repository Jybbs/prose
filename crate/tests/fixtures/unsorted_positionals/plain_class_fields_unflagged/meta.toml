[docs]
previewable = true
title       = "A Plain Class Draws No Report"

description = '''
A class whose header names no constructor generator binds no field by position, so the out-of-order run **draws no diagnostic**. [[alphabetize-siblings]] sorts these fields by name as an auto-fix instead.
'''

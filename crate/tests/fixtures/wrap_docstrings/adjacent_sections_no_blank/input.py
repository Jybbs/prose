def fetch(url):
    """
    Summary line.

    Args:
        url: the endpoint to fetch from, described at enough length that the entry wraps past its budget.
    Returns:
        the decoded payload.
    """
    return url

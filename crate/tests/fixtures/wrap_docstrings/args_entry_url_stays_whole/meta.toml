[docs]
previewable = false
title       = "An Over-Budget URL in an Entry Description Stays Whole"

description = '''
The `endpoint` entry under `Args:` carries a URL too wide for any line the budget allows. The wrap places it alone on a continuation line at the description column and lets it overflow there rather than splitting it at a `/` or `-`, leaving the address whole.
'''

[docs]
previewable = false
title       = "Blank Line Splits an Args Section into Adjacent Entries"

description = '''
A blank line ends the `first` entry's paragraph, so **the `second:` line below it is tested as a fresh entry candidate rather than as a continuation**. The `Args:` section composes as two adjacent entries, each wrapping at its own description column.
'''

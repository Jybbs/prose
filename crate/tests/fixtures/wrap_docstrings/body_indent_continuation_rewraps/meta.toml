[docs]
previewable = false
title       = "A Continuation at the Section Body Indent Re-Wraps to the Hanging Indent"

description = '''
The `foo` entry is already split across two source lines, its second line sitting at the section body indent rather than under the description. **The entry re-wraps with a hanging indent at the description column**, so a docstring wrapped by hand in the older layout ends up on the same column as one never wrapped at all.
'''

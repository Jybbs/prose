[docs]
previewable = false
title       = "A Bracketed Literal Below an Entry Stays Verbatim"

description = '''
A line opening on `{` or `[` reads as a literal rather than as prose, so it **passes through verbatim** and keeps its layout with no blank line separating it from the entry above. The description on the head line still reflows around it.
'''

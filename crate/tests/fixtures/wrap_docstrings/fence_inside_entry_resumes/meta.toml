[docs]
previewable = false
title       = "Fence Inside an Entry Passes Through and the Next Entry Resumes"

description = '''
The `sample` entry introduces a fenced code block, and **the fence passes through verbatim and ends the entry at the block's boundary**. Entry detection resumes once the closing fence lands, so the over-long `next_entry` description below it wraps with the hanging-column shape.
'''

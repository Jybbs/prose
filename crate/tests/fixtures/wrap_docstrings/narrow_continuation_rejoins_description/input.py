def configure():
    """
    Summary line.

    Args:
        name     : A descriptive parameter whose continuation lines sit at
              a narrower column than the one this entry now hangs from.
        encoding : The output encoding selected for the configured run.
    """

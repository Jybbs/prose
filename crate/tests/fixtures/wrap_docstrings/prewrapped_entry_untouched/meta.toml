[docs]
previewable = false
title       = "An Entry Already Wrapped With a Hanging Indent Passes Through Unchanged"

description = '''
The `first` entry under `Args:` is already wrapped at the docstring budget with its continuation lines at the hanging indent, and its sibling `second` fits on one line. **Both entries pass through unchanged**, because the rule writes an edit only where re-wrapping would change the output.
'''

[docs]
previewable = false
title       = "A Named `Yields:` Entry Wraps Like Every Other Entry"

description = '''
The `chunk` entry under `Yields:` runs past the budget. **It wraps at the docstring budget with a hanging indent at its description column**, so the continuation lines sit under the first word of the description, because a named yields entry takes the same form as every other entry-carrying section.
'''

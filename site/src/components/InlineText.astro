---
import { renderInline } from '../lib/markdown/render'

interface Props {
  text: string
}

// `text` carries inline markdown, rendered through the same plugin pipeline
// as page bodies.
const { text } = Astro.props
const html     = await renderInline(text)
---

<Fragment set:html={html} />

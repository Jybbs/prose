[docs]
previewable = false
title       = "A Cross-Line Dict Key Breaks the Run"

description = '''
One entry's key spans two lines, so its `:` lands on a different line from where the key opens. **The cross-line key breaks the run** rather than letting its neighbors reach across it, leaving the flat keys above it aligned and the key below it flush instead of stranded inside a shared column.
'''

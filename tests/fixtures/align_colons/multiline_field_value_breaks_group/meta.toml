[docs]
title = "A Multi-Line Field Value Breaks the Group"
description = '''
A class field whose value is a multi-line dict literal **breaks the alignment group**. The following single-line field lands in its own group rather than aligning across the multi-line entry, and the inner dict aligns independently.
'''
previewable = false

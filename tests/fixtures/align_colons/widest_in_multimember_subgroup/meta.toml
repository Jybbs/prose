[docs]
title = "Widest Field in a Shared Sub-Group Fires No Override"
description = '''
Class-field widths split into two sub-groups under `max-shift`. The widest field sits in a multi-member sub-group rather than alone, so **no anchor override fires** and each sub-group aligns at its own widest member's column.
'''
previewable = false

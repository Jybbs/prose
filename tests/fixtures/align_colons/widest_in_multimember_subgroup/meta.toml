[docs]
previewable = false
title       = "Widest Field in a Shared Sub-Group Aligns With It"

description = '''
Class-field widths split into two sub-groups under `max-shift`. The widest field sits in a multi-member sub-group rather than alone, so it **aligns with its neighbors** at that sub-group's widest column.
'''

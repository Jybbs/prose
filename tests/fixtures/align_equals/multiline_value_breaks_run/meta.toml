[docs]
previewable = false
title       = "A Multi-Line Value Ends the Keyword Run"

description = '''
A keyword whose value spans several lines **ends the run**, so the single-line keywords before it align while the multi-line keyword keeps its own spacing.
'''

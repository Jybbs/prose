[docs]
previewable = false
title       = "A Decorator-Pinned Function's Docstring Entries Keep Their Order"

description = '''
The docstring hold rides the same pin the signature does. A module-level function under a positional-binding decorator keeps its parameter order, and an `Args:` section whose every entry names one of those parameters **holds the same order**, mirroring the held signature.
'''

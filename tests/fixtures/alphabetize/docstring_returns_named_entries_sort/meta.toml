[docs]
title = "`Returns:` Entries Sort When Named"
description = '''
The `Returns:` section **reorders alphabetically** when each entry carries a `name: description` shape. Single-paragraph `Returns:` prose without entries falls through to the description path that `docstring-wrap` owns.
'''
previewable = false

[docs]
title = "`global` and `nonlocal` Name Lists Sort"
description = '''
`global` and `nonlocal` name lists **alphabetize**. Order is semantically meaningless because both declarations affect every listed name uniformly within the enclosing scope.
'''
previewable = false

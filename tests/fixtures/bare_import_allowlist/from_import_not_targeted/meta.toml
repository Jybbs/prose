[docs]
previewable = false
title       = "From-Imports Are Outside the Rule's Reach"

description = '''
`from os import path` carries no diagnostic because the rule inspects `Stmt::Import` and **never `Stmt::ImportFrom`**. The explicit member-import shape the rule nudges toward is already what this statement uses.
'''

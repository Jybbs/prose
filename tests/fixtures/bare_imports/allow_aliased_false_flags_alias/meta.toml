[docs]
previewable = false
title       = "Setting allow-aliased False Flags an Aliased Import"

description = '''
With `allow-aliased = false`, an aliased `import os as o` loses its exemption and flags once its alias namespace is read at most once, here the lone `o.getcwd()`.
'''

[docs]
previewable = true
title       = "A Custom Allow List Exempts Its Package"

description = '''
A sidecar `allow = ["torch"]` **keeps `torch` bare** whatever its read count, where an unlisted `numpy` read at most once still flags.
'''

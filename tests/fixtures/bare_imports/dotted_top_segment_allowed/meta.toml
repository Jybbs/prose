[docs]
previewable = true
title       = "Dotted Submodules Inherit the Exemption"

description = '''
`import numpy.linalg` is preserved because its **top-level segment `numpy` sits on the allow list**. Submodule access inherits the parent module's membership, so the full dotted path never triggers.
'''

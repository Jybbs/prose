[docs]
previewable = false
title       = "Setting exempt-aliased False Flags an Aliased Import"

description = '''
With `exempt-aliased = false`, an aliased `import os as o` loses its exemption and flags once its alias namespace is reached through a single attribute, here `o.getcwd()`.
'''

[docs]
previewable = true
title       = "Lint Diagnostics Never Rewrite the Source"

description = '''
A mix of a flagged single-use `os`, a broadly-used `torch`, and an aliased `json` passes through **byte-for-byte unchanged** across every run. The rule emits diagnostics only and never an edit.
'''

[docs]
previewable = false
title       = "Single-Line-Suite Classes Converge on Two Blanks"

description = '''
Two adjacent top-level classes written as single-line `pass` suites normalize to **two blank lines** between them and none above the first, converging on a second pass rather than oscillating.
'''

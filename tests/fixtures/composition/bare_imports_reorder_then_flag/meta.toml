[docs]
previewable = true
title       = "Bare Imports Reorder, Then Draw the Narrow-Use Lint"

description = '''
Three bare imports arrive out of order, each reached through a single attribute. `alphabetize` reorders the statements, and **`bare-imports` flags every one, each diagnostic anchored to the import as written** rather than to its post-reorder position.
'''

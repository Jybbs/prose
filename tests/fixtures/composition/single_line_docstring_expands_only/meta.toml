[docs]
previewable = true
title       = "Single-Line Method Docstrings Expand to Multi-Line"

description = '''
Methods each carry a single-line triple-quoted docstring. **`docstring-expand` and `docstring-frame` expand each into three-line form** with opener and closer on their own lines, leaving method spacing untouched.
'''

[docs]
title = "Every Knob Overridden Round-Trips Through the Loader"
description = '''
A `[tool.prose]` snippet that overrides **every available knob** to a non-default value, walking the top-level lengths, `target-version`, the `[tool.prose.cache]` toggles, and a per-rule override for each rule. The resolved `Config` carries every supplied value rather than any default.
'''
previewable = false

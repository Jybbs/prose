[docs]
title = "Named Returns Entries Wrap Like Args Entries"
description = '''
A long `Returns:` entry in the `name: description` shape **wraps at the docstring budget with a hanging indent at the description column**, matching the `Args:` entry shape for named-tuple-style return docs. A short sibling entry stays on its line.
'''
previewable = false

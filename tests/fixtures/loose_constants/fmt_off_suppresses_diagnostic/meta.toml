[docs]
title = "A `# fmt: off` Block Suppresses the Lint"
description = '''
A `# fmt: off` block carries the **same suppression weight against lint diagnostics as against edits**, so a constant inside the block escapes the rule's emission path entirely.
'''
previewable = true

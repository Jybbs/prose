[docs]
title = "Underscore-Prefixed Names Are Exempt by Default"
description = '''
A name matching the default `^_` allow pattern is **exempt**, so conventional placeholders like `_hidden` pass through silently. The leading underscore already signals deliberate disuse, which the rule honors out of the box.
'''
previewable = false

[docs]
title = "A `nonlocal` Declaration Skips the Function"
description = '''
A function whose body declares `nonlocal` is **skipped entirely**, because an accurate single-use reading would have to follow the binding out along the closure chain. The whole scope is exempted rather than analyzed in isolation.
'''
previewable = false

[docs]
title = "Off Block Spares a Trailing Comma the Filter Would Strip"
description = '''
The pipeline-level suppression filter covers **every rule, not just alignment**. A call inside a `# fmt: off` block keeps its trailing `,` while an identically shaped call outside the block has the comma stripped.
'''
previewable = false

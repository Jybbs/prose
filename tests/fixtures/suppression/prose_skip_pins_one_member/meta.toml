[docs]
previewable = false
title       = "Prose Skip Holds One Line Out, Mirroring Fmt Skip"

description = '''
A trailing `# prose: skip` **holds one line out of its alignment group** as a transparent gap, leaving every other row in the run to align to one shared column around it, mirroring bare `# fmt: skip` semantics.
'''

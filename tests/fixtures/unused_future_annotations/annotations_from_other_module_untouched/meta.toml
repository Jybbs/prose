[docs]
title = "A Lookalike Import from Another Module Is Untouched"
description = '''
A similarly-shaped `from typing import annotations` is left alone. The rule fires **only on `from __future__ import annotations`**, so the name match alone is not enough to trigger removal.
'''
previewable = false

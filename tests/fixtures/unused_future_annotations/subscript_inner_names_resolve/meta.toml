[docs]
title = "Subscripted Generic Inner Names Resolve"
description = '''
A subscripted generic annotation walks the `Subscript` into each inner `Name`. With both `Container` and `Item` defined above the annotation, **trigger 3 fires and the directive is removed**.
'''
previewable = false
